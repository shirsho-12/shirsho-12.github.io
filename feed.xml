<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.1">Jekyll</generator><link href="https://alshedivat.github.io/al-folio/feed.xml" rel="self" type="application/atom+xml"/><link href="https://alshedivat.github.io/al-folio/" rel="alternate" type="text/html" hreflang="en"/><updated>2022-12-21T11:26:53+00:00</updated><id>https://alshedivat.github.io/al-folio/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Temporal Difference Learning</title><link href="https://alshedivat.github.io/al-folio/blog/rl_td/" rel="alternate" type="text/html" title="Temporal Difference Learning"/><published>2021-06-30T00:00:00+00:00</published><updated>2021-06-30T00:00:00+00:00</updated><id>https://alshedivat.github.io/al-folio/blog/td</id><content type="html" xml:base="https://alshedivat.github.io/al-folio/blog/rl_td/"><![CDATA[<p>Temporal Difference Learning - the holy amalgamation of Monte Carlo and dynamic programming. Taking the best of both worlds, TD learning is a faster, model-less, more accurate method of solving reinforcement learning problems. TD is a concept that was first developed in reinforcement learning, and only later branched to other fields such as computer vision.</p> <p>Temporal difference learning uses concepts from both MC and DP, while at the same time improving over them. TD methods:</p> <ul> <li>Learn directly from raw experience without a model or environment specifications - Similar to Monte Carlo</li> <li>Update estimates based on prior estimates without waiting for final outcomes (bootstrapping) - Similar to DP</li> <li>Use the same General Policy Iteration approach seen in all other methods.</li> <li>Works well in non-stationary environments (where probabilities change over time).</li> <li>Estimates are updated at every time step - unlike MC where estimates are updated at the end of an episode.</li> <li>Values converge to the maximum-likelihood estimate, a different statistical estimate. MC values converge to the minimum least-square error. The maximum-likelihood estimate is oftentimes more accurate.</li> </ul> <p>The nice thing about temporal difference learning is that the algorithms are very straight-forward and easy to implement. This is partly because TD algorithms build on Monte Carlo and dynamic programming reinforcement learning, and partly due to the intuitive nature of the concept itself.</p> <h1 id="td-evaluation">TD Evaluation</h1> <p>As usual, we will go through the two cases of TD algorithms: prediction and control.</p> <p>Sutton &amp; Barlo summarize the difference between TD and MC in a very succinct way. Suppose you are driving home from work. As you are leaving, you estimate that it will take 30 minutes to get home. As you reach your car it starts to rain. Traffic is slower in the rain, so you change your estimate to 40 minutes. 10 minutes later you have completed the highway portion of the journey in good time and change your total time estimate to 25 minutes. But then there is an accident in the road and you end up being forced into a congested lane. In the end the journey took 45 minutes.<sup><a href="#myfootnote1">1</a></sup></p> <p>Suppose the objective of our agent is to estimate the total travel time to drive home. If we were to use a Monte Carlo agent, learning would only begin after the episode ends. This is clearly not the most logical way to go about this task. Based on the event, you and I would update our estimations on the fly. That is exactly what TD does.</p> <p>The value function <code class="language-plaintext highlighter-rouge">V</code> for state <code class="language-plaintext highlighter-rouge">s</code> at time <code class="language-plaintext highlighter-rouge">t</code> is calculated using Monte Carlo by</p> <p><img src="https://render.githubusercontent.com/render/math?math=V(s_t) \leftarrow V(s_t) %2B \alpha[G_t - V(s_t)]" class="center"/></p> <p>where <img src="https://render.githubusercontent.com/render/math?math=G_t"/> is the <strong>actual reward</strong> at time <code class="language-plaintext highlighter-rouge">t</code> and <code class="language-plaintext highlighter-rouge">α</code> is an hyperparameter. This update is done at the end of each episode because we do not know the reward before that. On the other hand, TD methods will only wait till time <code class="language-plaintext highlighter-rouge">t+1</code> to update their estimate.</p> <p><img src="https://render.githubusercontent.com/render/math?math=V(s_t) \leftarrow V(s_t) %2B \alpha[R_{t%2B1} %2B \gamma V(s_{t%2B1}) - V(s_t)]" class="center"/></p> <p>where <img src="https://render.githubusercontent.com/render/math?math=R_{t+1}"/> is the reward observed at time <code class="language-plaintext highlighter-rouge">t+1</code>. <code class="language-plaintext highlighter-rouge">γ</code> is the discounting rate. This update rule is known as <strong>TD(0)</strong>.</p> <h2 id="td0">TD(0)</h2> <p>TD(0) is the simplest form of temporal difference learning. The algorithm is as follows:</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="n">episode</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="n">num_episodes</span><span class="p">):</span>
    <span class="n">state</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">reset</span><span class="p">()</span>
    <span class="n">is_done</span> <span class="o">=</span> <span class="bp">False</span>
    <span class="k">while</span> <span class="ow">not</span> <span class="n">is_done</span><span class="p">:</span>
        <span class="n">action</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">argmax</span><span class="p">(</span><span class="n">pi</span><span class="p">[</span><span class="n">s</span><span class="p">])</span>
        <span class="n">next_state</span><span class="p">,</span> <span class="n">reward</span><span class="p">,</span> <span class="n">is_done</span><span class="p">,</span> <span class="n">info</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">step</span><span class="p">(</span><span class="n">action</span><span class="p">)</span>
        <span class="n">V</span><span class="p">[</span><span class="n">state</span><span class="p">]</span> <span class="o">=</span> <span class="n">V</span><span class="p">[</span><span class="n">state</span><span class="p">]</span> <span class="o">+</span> <span class="n">alpha</span> <span class="o">*</span> <span class="p">(</span><span class="n">reward</span> <span class="o">+</span> <span class="n">gamma</span> <span class="o">*</span> <span class="n">V</span><span class="p">[</span><span class="n">next_state</span><span class="p">]</span> <span class="o">-</span> <span class="n">V</span><span class="p">[</span><span class="n">state</span><span class="p">])</span>
        <span class="n">state</span> <span class="o">=</span> <span class="n">next_state</span>
</code></pre></div></div> <p>TD(0) is a simple value function update performed by ` V[state] + alpha * (reward + gamma * V[next_state] - V[state])`</p> <h1 id="td-control">TD Control</h1> <p>Just like Monte Carlo RL, there are two branches to temporal difference learning: on-policy and off-policy. We will go through both branches, and then look at the Gym’s <code class="language-plaintext highlighter-rouge">Taxi-v3</code> environment.</p> <h2 id="sarsa-on-policy-control">SARSA: On-policy Control</h2> <p>SARSA is an on-policy temporal difference learning algorithm. This means that we will use an ϵ-greedy policy to select our actions and the SARSA update rule to update our values. In this case we do not use values, but rather the Q-function. The reason is the same as Monte Carlo; we lack access to the environment probabilities (i.e. the Markov matrix).</p> <p>SARSA is named after the values needed in the equation. Namely state-action (present), reward, state-action(future). This becomes clear after seeing the equation.</p> <p>For a state <code class="language-plaintext highlighter-rouge">s</code>, action <code class="language-plaintext highlighter-rouge">a</code>, and reward <code class="language-plaintext highlighter-rouge">R</code>, the Q-function at time <code class="language-plaintext highlighter-rouge">t</code> is given by</p> <p><img src="https://render.githubusercontent.com/render/math?math=Q(s_t, a_t) \leftarrow Q(s_t, a_t) %2B \alpha[R_{t%2B1} %2B \gamma Q(s_{t%2B1}, a_{t%2B1}) - Q(s_t, a_t)]" class="center"/></p> <p>Yes, this equation is the TD(0) equation with V replaced by Q. The Python implementation looks like this</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">state</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">reset</span><span class="p">()</span>
<span class="n">is_done</span> <span class="o">=</span> <span class="bp">False</span>
<span class="n">action</span> <span class="o">=</span> <span class="n">eps_policy</span><span class="p">(</span><span class="n">state</span><span class="p">,</span> <span class="n">Q</span><span class="p">)</span>
<span class="k">while</span> <span class="ow">not</span> <span class="n">is_done</span><span class="p">:</span>
    <span class="n">next_state</span><span class="p">,</span> <span class="n">reward</span><span class="p">,</span> <span class="n">is_done</span><span class="p">,</span> <span class="n">info</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">step</span><span class="p">(</span><span class="n">action</span><span class="p">)</span>
    <span class="n">next_action</span> <span class="o">=</span> <span class="n">eps_policy</span><span class="p">(</span><span class="n">next_state</span><span class="p">,</span> <span class="n">Q</span><span class="p">)</span>
    <span class="n">del_td</span> <span class="o">=</span> <span class="n">reward</span> <span class="o">+</span> <span class="n">gamma</span> <span class="o">*</span> <span class="n">Q</span><span class="p">[</span><span class="n">next_state</span><span class="p">][</span><span class="n">next_action</span><span class="p">]</span> <span class="o">-</span> <span class="n">Q</span><span class="p">[</span><span class="n">state</span><span class="p">][</span><span class="n">action</span><span class="p">]</span>
    <span class="n">Q</span><span class="p">[</span><span class="n">state</span><span class="p">][</span><span class="n">action</span><span class="p">]</span> <span class="o">+=</span> <span class="n">alpha</span> <span class="o">*</span> <span class="n">del_td</span>
</code></pre></div></div> <p>Here, <code class="language-plaintext highlighter-rouge">eps_policy</code> is the ϵ-greedy action selection. <code class="language-plaintext highlighter-rouge">del_td</code> is a placeholder for the equation <code class="language-plaintext highlighter-rouge">Q[state][action] = Q[state][action] + alpha * (reward + gamma * Q[next_state][next_action] - Q[state][action])</code>. This is a very simple, yet powerful algorithm that often outperforms MC.</p> <h2 id="q-learning-the-two-off-policy-controls">Q-Learning: The Two Off-policy Controls</h2> <p>Q-Learning<sup><a href="#myfootnote2">2</a></sup> is an off-policy algorithm for TD control. This means we use a <em>behavior policy</em> to train a <em>target policy</em>. The same conditions for the behavior policy apply.</p> <p>The Q-learning update rule is given by the following equation.</p> <p><img src="https://render.githubusercontent.com/render/math?math=Q(s_t, a_t) \leftarrow Q(s_t, a_t) %2B \alpha[R_{t%2B1} %2B \gamma \max_{a}Q(s_{t%2B1}, a) - Q(s_t, a_t)]" class="center"/></p> <p>This is known as the <em>one-step Q-learning</em> algorithm.<img src="https://render.githubusercontent.com/render/math?math=\max_{a}Q(s_{t%2B1}, a)"/> means that we choose the action that gives the highest Q-value. Again, the equation is a lot simpler in implementation.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">state</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">reset</span><span class="p">()</span>
<span class="n">is_done</span> <span class="o">=</span> <span class="bp">False</span>
<span class="k">while</span> <span class="ow">not</span> <span class="n">is_done</span><span class="p">:</span>
    <span class="c1"># Off-policy
</span>    <span class="n">action</span> <span class="o">=</span> <span class="n">behaviour_policy</span><span class="p">(</span><span class="n">state</span><span class="p">,</span> <span class="n">Q</span><span class="p">)</span>
    <span class="n">next_state</span><span class="p">,</span> <span class="n">reward</span><span class="p">,</span> <span class="n">is_done</span><span class="p">,</span> <span class="n">info</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">step</span><span class="p">(</span><span class="n">action</span><span class="p">)</span>
    <span class="c1"># Time step update
</span>    <span class="n">del_td</span> <span class="o">=</span> <span class="n">reward</span> <span class="o">+</span> <span class="n">gamma</span> <span class="o">*</span> <span class="n">torch</span><span class="p">.</span><span class="nb">max</span><span class="p">(</span><span class="n">Q</span><span class="p">[</span><span class="n">next_state</span><span class="p">])</span> <span class="o">-</span> <span class="n">Q</span><span class="p">[</span><span class="n">state</span><span class="p">][</span><span class="n">action</span><span class="p">]</span>   
    <span class="n">Q</span><span class="p">[</span><span class="n">state</span><span class="p">][</span><span class="n">action</span><span class="p">]</span> <span class="o">+=</span> <span class="n">alpha</span> <span class="o">*</span> <span class="n">del_td</span>
    <span class="n">state</span> <span class="o">=</span> <span class="n">next_state</span>
</code></pre></div></div> <p>The main change to the update rule is <code class="language-plaintext highlighter-rouge">torch.max(Q[next_state])</code> instead of the <code class="language-plaintext highlighter-rouge">Q[next_state][next_action]</code> in SARSA.</p> <p>While one-step Q-learning is powerful, it suffers from overestimation of action values due to its greediness (<code class="language-plaintext highlighter-rouge">max</code>).</p> <h3 id="improvement-double-q-learning">Improvement: Double Q-Learning</h3> <p>So how could we improve the Q-learning algorithm? In 2010, Hado van Hasselt proposed a simple, yet powerful improvement: <em>If one Q-learner fails, use two.</em> This was the advent of <strong>Double Q-learning</strong> <sup><a href="#myfootnote3">3</a></sup>.</p> <p>The algorithm is as follows: use two Q-functions. At each iteration, take the greedy action from both Q-functions and choose one randomly. If the action <code class="language-plaintext highlighter-rouge">a</code> is selected from Q-function <code class="language-plaintext highlighter-rouge">Q1</code>, Q1 is updated, otherwise Q2 is updated. The update rule for Q1 is</p> <p><img src="https://render.githubusercontent.com/render/math?math=Q1(s_t, a_t) \leftarrow Q1(s_t, a_t) %2B \alpha[R_{t%2B1} %2B \gamma \max_{a}Q2(s_{t%2B1}, a) - Q1(s_t, a_t)]" class="center"/></p> <p>Similarly, the update rule when the action is selected from Q2 is</p> <p><img src="https://render.githubusercontent.com/render/math?math=Q2(s_t, a_t) \leftarrow Q2(s_t, a_t) %2B \alpha[R_{t%2B1} %2B \gamma \max_{a}Q1(s_{t%2B1}, a) - Q2(s_t, a_t)]" class="center"/></p> <p>In practice, the action to be taken <img src="https://render.githubusercontent.com/render/math?math=a_t"/> is selected using the ϵ-greedy policy with <code class="language-plaintext highlighter-rouge">Q1 + Q2</code>. We randomly choose the Q-function for the future estimate <img src="https://render.githubusercontent.com/render/math?math=a_{t%2B 1}"/>. The implementation is also slightly more complicated.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">state</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">reset</span><span class="p">()</span>
<span class="n">is_done</span> <span class="o">=</span> <span class="bp">False</span>
<span class="k">while</span> <span class="ow">not</span> <span class="n">is_done</span><span class="p">:</span>
    <span class="n">action</span> <span class="o">=</span> <span class="n">eps_policy</span><span class="p">(</span><span class="n">state</span><span class="p">,</span> <span class="n">Q1</span> <span class="o">+</span> <span class="n">Q2</span><span class="p">)</span>
    <span class="n">next_state</span><span class="p">,</span> <span class="n">reward</span><span class="p">,</span> <span class="n">is_done</span><span class="p">,</span> <span class="n">info</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">step</span><span class="p">(</span><span class="n">action</span><span class="p">)</span>
    <span class="k">if</span> <span class="n">torch</span><span class="p">.</span><span class="n">rand</span><span class="p">(</span><span class="mi">1</span><span class="p">).</span><span class="n">item</span><span class="p">()</span> <span class="o">&lt;</span> <span class="mf">0.5</span><span class="p">:</span>             <span class="c1"># update 1 randomly
</span>        <span class="n">best_next_action</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">argmax</span><span class="p">(</span><span class="n">Q1</span><span class="p">[</span><span class="n">next_state</span><span class="p">])</span>
        <span class="n">del_td</span> <span class="o">=</span> <span class="n">reward</span> <span class="o">+</span> <span class="n">gamma</span> <span class="o">*</span> <span class="n">Q2</span><span class="p">[</span><span class="n">next_state</span><span class="p">][</span><span class="n">best_next_action</span><span class="p">]</span> <span class="o">-</span> <span class="n">Q1</span><span class="p">[</span><span class="n">state</span><span class="p">][</span><span class="n">action</span><span class="p">]</span>
        <span class="n">Q1</span><span class="p">[</span><span class="n">state</span><span class="p">][</span><span class="n">action</span><span class="p">]</span> <span class="o">+=</span> <span class="n">del_td</span> <span class="o">*</span> <span class="n">alpha</span>
    <span class="k">else</span><span class="p">:</span>
        <span class="n">best_next_action</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">argmax</span><span class="p">(</span><span class="n">Q2</span><span class="p">[</span><span class="n">next_state</span><span class="p">])</span>
        <span class="n">del_td</span> <span class="o">=</span> <span class="n">reward</span> <span class="o">+</span> <span class="n">gamma</span> <span class="o">*</span> <span class="n">Q1</span><span class="p">[</span><span class="n">next_state</span><span class="p">][</span><span class="n">best_next_action</span><span class="p">]</span> <span class="o">-</span> <span class="n">Q2</span><span class="p">[</span><span class="n">state</span><span class="p">][</span><span class="n">action</span><span class="p">]</span>
        <span class="n">Q2</span><span class="p">[</span><span class="n">state</span><span class="p">][</span><span class="n">action</span><span class="p">]</span> <span class="o">+=</span> <span class="n">del_td</span> <span class="o">*</span> <span class="n">alpha</span>
    <span class="n">state</span> <span class="o">=</span> <span class="n">next_state</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">torch.rand(1)</code> is used to randomly select an action (the function returns a value between 0 and 1). When the future action is selected from Q1, the update rule becomes <code class="language-plaintext highlighter-rouge">reward + gamma * Q2[next_state][best_next_action] - Q1[state][action]</code>. Notice the Q-function Q2 is used for the future action. At the end of the simulations, the final Q-function <code class="language-plaintext highlighter-rouge">Q</code> is the sum of Q1 and Q2.</p> <p>Double-Q learning is slightly harder to implement, but is more robust and accurate.</p> <h1 id="hailing-a-taxi">Hailing A Taxi</h1> <p>Now that we have looked at the basic temporal difference learning algorithms, let’s solve an environment.</p> <h2 id="the-scenario">The Scenario</h2> <p>You are a taxi driver who has to pick up and drop off passengers on a <img src="https://render.githubusercontent.com/render/math?math=5 \times 5"/> grid. There a 6 possible actions (4 movement directions, pick up and drop off), pick up, drop off and taxi positions are randomly selected. 4 possible pick up (+1 if already in taxi) and drop off positions (on the sides of the board) lead to <img src="https://render.githubusercontent.com/render/math?math=25 \times 5 \times 4=500"/> states.</p> <figure> <img src="/images/rl/taxi_order.jpg" desc="Taxi meme"/> <figcaption>Well did you?</figcaption> </figure> <p>Rewards:</p> <ul> <li>-1 per time step</li> <li>-10 for each illegal pick up/drop off</li> <li>+20 for a successful drop off.</li> </ul> <p>Let’s first import the necessary libraries.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">gym</span>
<span class="kn">import</span> <span class="nn">matplotlib.pyplot</span> <span class="k">as</span> <span class="n">plt</span> 
<span class="kn">import</span> <span class="nn">torch</span>
<span class="kn">from</span> <span class="nn">collections</span> <span class="kn">import</span> <span class="n">defaultdict</span>
<span class="kn">from</span> <span class="nn">tqdm</span> <span class="kn">import</span> <span class="n">tqdm</span>
</code></pre></div></div> <p>On to starting the environment.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">env</span> <span class="o">=</span> <span class="n">gym</span><span class="p">.</span><span class="n">make</span><span class="p">(</span><span class="s">"Taxi-v3"</span><span class="p">)</span>
<span class="n">num_actions</span><span class="p">,</span> <span class="n">num_states</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">action_space</span><span class="p">.</span><span class="n">n</span><span class="p">,</span> <span class="n">env</span><span class="p">.</span><span class="n">observation_space</span><span class="p">.</span><span class="n">n</span>
<span class="k">print</span><span class="p">(</span><span class="n">num_actions</span><span class="p">,</span> <span class="n">num_states</span><span class="p">)</span>
<span class="o">&gt;&gt;&gt;</span> <span class="mi">6</span> <span class="mi">500</span>

<span class="n">env</span><span class="p">.</span><span class="n">reset</span><span class="p">()</span>
<span class="n">env</span><span class="p">.</span><span class="n">render</span><span class="p">()</span>
</code></pre></div></div> <figure> <img src="/images/rl/taxi_env.png" desc="Taxi render output"/> </figure> <p>The output of <code class="language-plaintext highlighter-rouge">env.render()</code> represents the grid. The passenger is picked up and dropped off on one of the following: red(R), green(G), yellow (Y), and blue(B). In the image above, the passenger is on B (colored blue) and the passenger is on Y (colored magenta). If the taxi is full it is rendered green, otherwise it is yellow. This time, we do not need to create a separate method to run an episode; the agent is trained in each time step.</p> <figure> <img src="/images/rl/taxi_base.gif" desc="A random Taxi-v3 simulation" class="center"/> <figcaption>Here the agent is randomly selecting moves. Someday the taxi might reach its destination, but I wouldn't count on it.</figcaption> </figure> <h2 id="sarsa-taxis">SARSA Taxis</h2> <p>Let’s first use SARSA to train our agent. For the ϵ-greedy exploration policy, we implement a separate function.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">gen_eps_greedy_policy</span><span class="p">(</span><span class="n">num_actions</span><span class="p">,</span> <span class="n">epsilon</span><span class="p">):</span>
    <span class="c1"># Epsilon Greedy exploratory policy 
</span>    <span class="k">def</span> <span class="nf">policy_function</span><span class="p">(</span><span class="n">state</span><span class="p">,</span> <span class="n">Q</span><span class="p">):</span>
        <span class="n">probs</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">ones</span><span class="p">(</span><span class="n">num_actions</span><span class="p">)</span> <span class="o">*</span> <span class="n">epsilon</span> <span class="o">/</span> <span class="n">num_actions</span>
        <span class="n">best_action</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">argmax</span><span class="p">(</span><span class="n">Q</span><span class="p">[</span><span class="n">state</span><span class="p">]).</span><span class="n">item</span><span class="p">()</span>
        <span class="n">probs</span><span class="p">[</span><span class="n">best_action</span><span class="p">]</span> <span class="o">+=</span> <span class="mi">1</span> <span class="o">-</span> <span class="n">epsilon</span>
        <span class="n">action</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">multinomial</span><span class="p">(</span><span class="n">probs</span><span class="p">,</span> <span class="mi">1</span><span class="p">).</span><span class="n">item</span><span class="p">()</span>
        <span class="k">return</span> <span class="n">action</span>
    <span class="k">return</span> <span class="n">policy_function</span>
</code></pre></div></div> <p>This form of function encapsulation is the same as what we did in the behavior policy generation function before. The probabilities are calculated and an action is selected from the multinomial distribution.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">sarsa</span><span class="p">(</span><span class="n">env</span><span class="p">,</span> <span class="n">gamma</span><span class="p">,</span> <span class="n">num_episodes</span><span class="p">,</span> <span class="n">alpha</span><span class="p">,</span> <span class="n">eps_policy</span><span class="p">):</span>
    <span class="n">num_actions</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">action_space</span><span class="p">.</span><span class="n">n</span>
    <span class="n">Q</span> <span class="o">=</span> <span class="n">defaultdict</span><span class="p">(</span><span class="k">lambda</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">zeros</span><span class="p">(</span><span class="n">num_actions</span><span class="p">))</span>
    <span class="k">for</span> <span class="n">episode</span> <span class="ow">in</span> <span class="n">tqdm</span><span class="p">(</span><span class="nb">range</span><span class="p">(</span><span class="n">num_episodes</span><span class="p">)):</span>
        <span class="n">state</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">reset</span><span class="p">()</span>
        <span class="n">is_done</span> <span class="o">=</span> <span class="bp">False</span>
        <span class="n">action</span> <span class="o">=</span> <span class="n">eps_policy</span><span class="p">(</span><span class="n">state</span><span class="p">,</span> <span class="n">Q</span><span class="p">)</span>
        <span class="k">while</span> <span class="ow">not</span> <span class="n">is_done</span><span class="p">:</span>
            <span class="n">next_state</span><span class="p">,</span> <span class="n">reward</span><span class="p">,</span> <span class="n">is_done</span><span class="p">,</span> <span class="n">info</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">step</span><span class="p">(</span><span class="n">action</span><span class="p">)</span>
            <span class="n">next_action</span> <span class="o">=</span> <span class="n">eps_policy</span><span class="p">(</span><span class="n">next_state</span><span class="p">,</span> <span class="n">Q</span><span class="p">)</span>
            <span class="n">del_td</span> <span class="o">=</span> <span class="n">reward</span> <span class="o">+</span> <span class="n">gamma</span> <span class="o">*</span> <span class="n">Q</span><span class="p">[</span><span class="n">next_state</span><span class="p">][</span><span class="n">next_action</span><span class="p">]</span> <span class="o">-</span> <span class="n">Q</span><span class="p">[</span><span class="n">state</span><span class="p">][</span><span class="n">action</span><span class="p">]</span>
            <span class="n">Q</span><span class="p">[</span><span class="n">state</span><span class="p">][</span><span class="n">action</span><span class="p">]</span> <span class="o">+=</span> <span class="n">alpha</span> <span class="o">*</span> <span class="n">del_td</span>
            <span class="n">state</span> <span class="o">=</span> <span class="n">next_state</span>
            <span class="n">action</span> <span class="o">=</span> <span class="n">next_action</span>
    <span class="n">policy</span> <span class="o">=</span> <span class="p">{}</span>
    <span class="k">for</span> <span class="n">state</span><span class="p">,</span> <span class="n">actions</span> <span class="ow">in</span> <span class="n">Q</span><span class="p">.</span><span class="n">items</span><span class="p">():</span>
        <span class="n">policy</span><span class="p">[</span><span class="n">state</span><span class="p">]</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">argmax</span><span class="p">(</span><span class="n">actions</span><span class="p">).</span><span class="n">item</span><span class="p">()</span>
    <span class="k">return</span> <span class="n">Q</span><span class="p">,</span> <span class="n">policy</span>
</code></pre></div></div> <p>Here we run the simulation <code class="language-plaintext highlighter-rouge">num_episodes</code> number of times. In each time step of a simulation, we update the Q-function value for a state-action pair by <code class="language-plaintext highlighter-rouge">alpha * del_td</code> where <code class="language-plaintext highlighter-rouge">del_td</code> is the SARSA update. In the end we extract the optimal policy from the Q-function using <code class="language-plaintext highlighter-rouge">torch.argmax</code>.</p> <h3 id="results">Results</h3> <p>To visualize results, we first need a method to plot them. For this we will use <code class="language-plaintext highlighter-rouge">matplotlib</code>.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">plot_rate</span><span class="p">(</span><span class="n">episode_length</span><span class="p">,</span> <span class="n">total_reward_episode</span><span class="p">):</span>
    <span class="n">fig</span><span class="p">,</span> <span class="n">ax</span> <span class="o">=</span> <span class="n">plt</span><span class="p">.</span><span class="n">subplots</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="n">figsize</span><span class="o">=</span><span class="p">(</span><span class="mi">12</span><span class="p">,</span> <span class="mi">6</span><span class="p">))</span>
    <span class="n">ax</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">plot</span><span class="p">(</span><span class="n">episode_length</span><span class="p">)</span>
    <span class="n">ax</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">set_title</span><span class="p">(</span><span class="s">"Episode Length over time"</span><span class="p">)</span>
    <span class="n">ax</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="nb">set</span><span class="p">(</span><span class="n">xlabel</span><span class="o">=</span><span class="s">"Episode"</span><span class="p">,</span> <span class="n">ylabel</span><span class="o">=</span><span class="s">"Length"</span><span class="p">)</span>
    <span class="n">ax</span><span class="p">[</span><span class="mi">1</span><span class="p">].</span><span class="n">plot</span><span class="p">(</span><span class="n">total_reward_episode</span><span class="p">)</span>
    <span class="n">ax</span><span class="p">[</span><span class="mi">1</span><span class="p">].</span><span class="n">set_title</span><span class="p">(</span><span class="s">"Episode reward over time"</span><span class="p">)</span>
    <span class="n">ax</span><span class="p">[</span><span class="mi">1</span><span class="p">].</span><span class="nb">set</span><span class="p">(</span><span class="n">xlabel</span><span class="o">=</span><span class="s">"Episode reward over time"</span><span class="p">,</span> <span class="n">ylabel</span><span class="o">=</span><span class="s">"Reward"</span><span class="p">)</span>

    <span class="n">plt</span><span class="p">.</span><span class="n">show</span><span class="p">()</span>
</code></pre></div></div> <p>This is a very simple function that uses two subplots (labeled <code class="language-plaintext highlighter-rouge">ax[0]</code> and <code class="language-plaintext highlighter-rouge">ax[1]</code>) to visualize episode lengths and episode rewards.</p> <p>On to running our algorithm.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">num_episodes</span> <span class="o">=</span> <span class="mi">1000</span>
<span class="n">episode_length</span> <span class="o">=</span> <span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">*</span> <span class="n">num_episodes</span>
<span class="n">total_reward_episode</span> <span class="o">=</span> <span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">*</span> <span class="n">num_episodes</span>

<span class="n">gamma</span> <span class="o">=</span> <span class="mi">1</span>
<span class="n">alpha</span> <span class="o">=</span> <span class="mf">0.6</span>
<span class="n">epsilon</span> <span class="o">=</span> <span class="mf">0.01</span>        <span class="c1"># Grid Search parameters
</span><span class="n">eps_policy</span> <span class="o">=</span> <span class="n">gen_eps_greedy_policy</span><span class="p">(</span><span class="n">env</span><span class="p">.</span><span class="n">action_space</span><span class="p">.</span><span class="n">n</span><span class="p">,</span> <span class="n">epsilon</span><span class="p">)</span>
<span class="n">optimal_Q</span><span class="p">,</span> <span class="n">optimal_policy</span> <span class="o">=</span> <span class="n">sarsa</span><span class="p">(</span><span class="n">env</span><span class="p">,</span> <span class="n">gamma</span><span class="p">,</span> <span class="n">num_episodes</span><span class="p">,</span> <span class="n">alpha</span><span class="p">,</span> <span class="n">eps_policy</span><span class="p">)</span>

<span class="n">plot_rate</span><span class="p">(</span><span class="n">episode_length</span><span class="p">,</span> <span class="n">total_reward_episode</span><span class="p">)</span>
</code></pre></div></div> <p>γ is set to 1 because the previous action directly affects the next one. To find the best epsilon and gamma, you need to run the experiment many times. This part is mostly trial and error. Add the following lines within the <code class="language-plaintext highlighter-rouge">while not is_done</code> loop to make plots.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">total_reward_episode</span><span class="p">[</span><span class="n">episode</span><span class="p">]</span> <span class="o">+=</span> <span class="n">reward</span>
<span class="n">episode_length</span><span class="p">[</span><span class="n">episode</span><span class="p">]</span> <span class="o">+=</span> <span class="mi">1</span>
</code></pre></div></div> <p>We then plot the results.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">plot_rate</span><span class="p">(</span><span class="n">episode_length</span><span class="p">,</span> <span class="n">total_reward_episode</span><span class="p">)</span>
</code></pre></div></div> <figure> <img src="/images/rl/sarsa.png" desc="SARSA results on Taxi-v3"/> <figcaption>Episode length and reward over time for the SARSA algorithm in Taxi-v3. The policy stabilizes very well in 400 episodes.</figcaption> </figure> <p>SARSA works very well with the Taxi-v3 environment. The agent reaches the optimal policy by 400 episodes. There is some fluctuation as the passenger position is always random. The fact that we have the optimal hyperparameters should also be noted.</p> <p>In summary, SARSA is great for the problem.</p> <figure> <img src="/images/rl/taxi.gif" desc="Solved Taxi-v3 simulation" class="center"/> <figcaption>The agent following the SARSA optimal policy. See how few the number of moves are.</figcaption> </figure> <h2 id="q-learning-the-cab">Q-Learning the Cab</h2> <p>Let’s now head over to Q-learning. We will again use the ϵ-greedy policy as our behavior policy.</p> <p>These are our parameters. The <code class="language-plaintext highlighter-rouge">episode_length</code> and <code class="language-plaintext highlighter-rouge">total_reward_episode</code> arrays are required for visualizations.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">num_episodes</span> <span class="o">=</span> <span class="mi">1000</span>
<span class="n">episode_length</span> <span class="o">=</span> <span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">*</span> <span class="n">num_episodes</span>
<span class="n">total_reward_episode</span> <span class="o">=</span> <span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">*</span> <span class="n">num_episodes</span>

<span class="n">gamma</span> <span class="o">=</span> <span class="mi">1</span>
<span class="n">alpha</span> <span class="o">=</span> <span class="mf">0.4</span>
<span class="n">epsilon</span> <span class="o">=</span> <span class="mf">0.1</span>
<span class="n">behaviour_policy</span> <span class="o">=</span> <span class="n">gen_eps_greedy_policy</span><span class="p">(</span><span class="n">env</span><span class="p">.</span><span class="n">action_space</span><span class="p">.</span><span class="n">n</span><span class="p">,</span> <span class="n">epsilon</span><span class="p">)</span>
</code></pre></div></div> <p>On to the Q-learning algorithm.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">q_learning</span><span class="p">(</span><span class="n">env</span><span class="p">,</span> <span class="n">behaviour_policy</span><span class="p">,</span> <span class="n">gamma</span><span class="p">,</span> <span class="n">num_episodes</span><span class="p">,</span> <span class="n">alpha</span><span class="p">):</span>
    <span class="n">num_actions</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">action_space</span><span class="p">.</span><span class="n">n</span>
    <span class="n">Q</span> <span class="o">=</span> <span class="n">defaultdict</span><span class="p">(</span><span class="k">lambda</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">zeros</span><span class="p">(</span><span class="n">num_actions</span><span class="p">))</span>
    <span class="k">for</span> <span class="n">episode</span> <span class="ow">in</span> <span class="n">tqdm</span><span class="p">(</span><span class="nb">range</span><span class="p">(</span><span class="n">num_episodes</span><span class="p">)):</span>
        <span class="n">state</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">reset</span><span class="p">()</span>
        <span class="n">is_done</span> <span class="o">=</span> <span class="bp">False</span>
        <span class="k">while</span> <span class="ow">not</span> <span class="n">is_done</span><span class="p">:</span>
            <span class="n">action</span> <span class="o">=</span> <span class="n">behaviour_policy</span><span class="p">(</span><span class="n">state</span><span class="p">,</span> <span class="n">Q</span><span class="p">)</span>
            <span class="n">next_state</span><span class="p">,</span> <span class="n">reward</span><span class="p">,</span> <span class="n">is_done</span><span class="p">,</span> <span class="n">info</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">step</span><span class="p">(</span><span class="n">action</span><span class="p">)</span>
            <span class="c1"># Time step update
</span>            <span class="n">del_td</span> <span class="o">=</span> <span class="n">reward</span> <span class="o">+</span> <span class="n">gamma</span> <span class="o">*</span> <span class="n">torch</span><span class="p">.</span><span class="nb">max</span><span class="p">(</span><span class="n">Q</span><span class="p">[</span><span class="n">next_state</span><span class="p">])</span> <span class="o">-</span> <span class="n">Q</span><span class="p">[</span><span class="n">state</span><span class="p">][</span><span class="n">action</span><span class="p">]</span>   
            <span class="n">Q</span><span class="p">[</span><span class="n">state</span><span class="p">][</span><span class="n">action</span><span class="p">]</span> <span class="o">+=</span> <span class="n">alpha</span> <span class="o">*</span> <span class="n">del_td</span>
            <span class="n">state</span> <span class="o">=</span> <span class="n">next_state</span>
            <span class="n">total_reward_episode</span><span class="p">[</span><span class="n">episode</span><span class="p">]</span> <span class="o">+=</span> <span class="n">reward</span>
            <span class="n">episode_length</span><span class="p">[</span><span class="n">episode</span><span class="p">]</span> <span class="o">+=</span> <span class="mi">1</span>
    <span class="n">policy</span> <span class="o">=</span> <span class="p">{}</span>
    <span class="k">for</span> <span class="n">state</span><span class="p">,</span> <span class="n">actions</span> <span class="ow">in</span> <span class="n">Q</span><span class="p">.</span><span class="n">items</span><span class="p">():</span>
        <span class="n">policy</span><span class="p">[</span><span class="n">state</span><span class="p">]</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">argmax</span><span class="p">(</span><span class="n">actions</span><span class="p">).</span><span class="n">item</span><span class="p">()</span>
    <span class="k">return</span> <span class="n">Q</span><span class="p">,</span> <span class="n">policy</span>
</code></pre></div></div> <p>Notice the main difference in the two algorithms. Q-learning uses <code class="language-plaintext highlighter-rouge">torch.max</code>. This one line makes it off-policy because there is no guarantee we will be using the action output by <code class="language-plaintext highlighter-rouge">torch.max</code> in the next time step.</p> <h1 id="results-1">Results</h1> <p>Just like last time, we will be using the <code class="language-plaintext highlighter-rouge">plot_rate</code> method to plot the graphs.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">plot_rate</span><span class="p">(</span><span class="n">episode_length</span><span class="p">,</span> <span class="n">total_reward_episode</span><span class="p">)</span>
</code></pre></div></div> <figure> <img src="/images/rl/q-learning.png" desc="Q-learning results on Taxi-v3"/> <figcaption>Episode length and reward over time for the Q-learning algorithm in Taxi-v3. Although the policy stabilizes by 400 episodes, the fluctuations are larger.</figcaption> </figure> <p>As stated before, Q-learning is prone to weaker results due to its greedy nature. This is clearly visible here, as the fluctuations in rewards and lengths are larger than that of SARSA. An improvement to this is double Q-learning. Feel free to try that out with the algorithm given above. Double Q-learning is slower to learn because it uses two Q-functions at once. Increasing the number of episodes to 3000 will give good results.</p> <h3 id="conclusion">Conclusion</h3> <p>Temporal difference learning contains some of the most powerful reinforcement learning algorithms we have seen yet. It is a concept that has its roots deep in RL. TD uses concepts from both Monte Carlo and reinforcement learning, making this strategy very versatile.</p> <p>TD algorithms sample the expected value of <code class="language-plaintext highlighter-rouge">V</code> (MC sampling) and use this estimate to find the optimal value function (bootstrapping). This estimations on estimates may seem shaky, but due to the maximum-likelihood estimations the algorithms make, the results tend to be the most accurate.</p> <p>In this article we looked at some of the main temporal difference learning algorithms. Starting from evaluation using TD(0) to control using SARSA and Q-learning, all TD algorithms use straightforward ways to solve problems. This makes TD algorithms easy to implement.</p> <p>We also looked at <code class="language-plaintext highlighter-rouge">Taxi-v3</code>, a complex environment where the agent picks up and drops off passengers to and from random locations. This is the hallmark of a non-stationary problem. Some of the approaches we’d seen previously will most likely fail here, but temporal difference learning solves it easily. In fact, training takes about a minute in both the algorithms tried above.</p> <p>Next time, we will look a bit more at temporal difference learning and number of hybrid methods that use a mix of the concepts seen before. Namely, the next article will be on TD(γ), tabular methods and the actor-critic model. Deep reinforcement learning robots aren’t far off!</p> <h4 id="footnotes">Footnotes</h4> <p><a name="myfootnote1">[1]</a> Sutton, R. S., &amp; Barto, A. G. (2018). Reinforcement learning: An Introduction. MIT press.</p> <p><a name="myfootnote2">[2]</a> Watkins, C. J., &amp; Dayan, P. (1992). Q-learning. Machine learning, 8(3-4), 279-292.</p> <p><a name="myfootnote3">[3]</a> Hasselt, H. (2010). Double Q-learning. Advances in neural information processing systems, 23, 2613-2621.</p>]]></content><author><name></name></author><category term="Reinforcement Learning"/><category term="TD-0"/><category term="SARSA"/><category term="Q-Learning"/><category term="RL Basics"/><summary type="html"><![CDATA[Temporal Difference Learning - the holy amalgamation of Monte Carlo and dynamic programming. Taking the best of both worlds, TD learning is a faster, model-less, more accurate method of solving reinforcement learning problems. TD is a concept that was first developed in reinforcement learning, and only later branched to other fields such as computer vision.]]></summary></entry><entry><title type="html">Roulette - To Play or Not to Play</title><link href="https://alshedivat.github.io/al-folio/blog/rl_mc_roulette/" rel="alternate" type="text/html" title="Roulette - To Play or Not to Play"/><published>2021-06-25T00:00:00+00:00</published><updated>2021-06-25T00:00:00+00:00</updated><id>https://alshedivat.github.io/al-folio/blog/mc_roulette</id><content type="html" xml:base="https://alshedivat.github.io/al-folio/blog/rl_mc_roulette/"><![CDATA[<p>Previously, we looked into the Monte Carlo family of reinforcement learning algorithms. These methods estimate the solution to a RL problem by <em>learning from experience</em>. Just by simulation and blind exploration, Monte Carlo agent reaches the optimal solution.</p> <p>Today, we will go through an interesting case: the game of roulette. We will discover the optimal strategy to win at roulette and hopefully close down a few casinos in the process.</p> <h3 id="the-game">The Game</h3> <p>A roulette wheel consists of a spinning disk with divisions around its edge that revolves around the base of a bowl. A ball is spun around the outside of the bowl until eventually ball and wheel come to rest with the ball in one of the divisions. The divisions around the wheel are numbered from 1 to 36 in a seemingly random pattern and alternate red and black. Prior to rolling the ball, people place bets on what number will come up by laying down chips on a betting mat, the precise location of the chips indicating the bet being made<sup><a href="#myfootnote1">1</a></sup>.</p> <figure> <img src="/images/rl/roulette_wheel.jpg" desc="A roulette wheel"/> <figcaption>A roulette wheel in high definition.</figcaption> </figure> <h2 id="the-player">The Player</h2> <p>We will set up the game in OpenAI Gym’s <strong>Roulette-v0</strong> environment. Let’s first do the basic imports.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">torch</span>
<span class="kn">import</span> <span class="nn">gym</span>
</code></pre></div></div> <p>The agent (our algorithm) interacts with the environment we create here.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">env</span> <span class="o">=</span> <span class="n">gym</span><span class="p">.</span><span class="n">make</span><span class="p">(</span><span class="s">"Roulette-v0"</span><span class="p">)</span>
</code></pre></div></div> <p>Our agent will play the game a number of times, each time choosing to either bet on a number or walk away with its winnings. There are 4 sets of possible actions:</p> <ul> <li>The agent bets on an even number (except 0).</li> <li>The agent bets on an odd number.</li> <li>The agent bets on 0.</li> <li>The agent walks away from the board.</li> </ul> <p>There are 3 possible types of rewards for the agent’s action.</p> <ul> <li>The rolled number is not zero and its parity matches the agent’s bet -&gt; +1</li> <li>The rolled number’s parity does not match the agent’s bet -&gt; -1</li> <li>The rolled number and the agent’s parity are both zero -&gt; +36</li> </ul> <p>Betting on a zero is the highest risk, highest reward outcome.</p> <p>Our agent therefore has 38 possible actions to choose from - the numbers 0 to 36 and walking away (37).</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">num_actions</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">action_space</span><span class="p">.</span><span class="n">n</span>
<span class="k">print</span><span class="p">(</span><span class="n">num_actions</span><span class="p">)</span>
<span class="o">&gt;&gt;&gt;</span> <span class="mi">38</span>
</code></pre></div></div> <p>In the roulette simulation, the environment only has one state. This is because a game ends after one action (the bet). This state is denoted by 0.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">num_states</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">observation_space</span><span class="p">.</span><span class="n">n</span>
<span class="k">print</span><span class="p">(</span><span class="n">num_states</span><span class="p">)</span>
<span class="o">&gt;&gt;&gt;</span> <span class="mi">1</span>
<span class="n">state</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">reset</span><span class="p">()</span>
<span class="k">print</span><span class="p">(</span><span class="n">state</span><span class="p">)</span>
<span class="o">&gt;&gt;&gt;</span> <span class="mi">0</span>
</code></pre></div></div> <p>The outcome for an action can be -1, 1, 0 or 36.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">env</span><span class="p">.</span><span class="n">step</span><span class="p">(</span><span class="mi">1</span><span class="p">)</span>  <span class="c1"># Ball lands on odd number
</span><span class="o">&gt;&gt;&gt;</span> <span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="mf">1.0</span><span class="p">,</span> <span class="bp">False</span><span class="p">,</span> <span class="p">{})</span>
<span class="n">env</span><span class="p">.</span><span class="n">step</span><span class="p">(</span><span class="mi">1</span><span class="p">)</span>  <span class="c1"># Ball lands on even number / zero
</span><span class="o">&gt;&gt;&gt;</span> <span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="o">-</span><span class="mf">1.0</span><span class="p">,</span> <span class="bp">False</span><span class="p">,</span> <span class="p">{})</span>
<span class="n">env</span><span class="p">.</span><span class="n">step</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>  <span class="c1"># Ball lands on zero
</span><span class="o">&gt;&gt;&gt;</span> <span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="mf">36.0</span><span class="p">,</span> <span class="bp">False</span><span class="p">,</span> <span class="p">{})</span>
<span class="n">env</span><span class="p">.</span><span class="n">step</span><span class="p">(</span><span class="mi">37</span><span class="p">)</span> <span class="c1"># Agent walks away
</span><span class="o">&gt;&gt;&gt;</span> <span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="mf">0.0</span><span class="p">,</span> <span class="bp">True</span><span class="p">,</span> <span class="p">{})</span>
</code></pre></div></div> <p>The game will end if the agent walks away (action 37) or after 100 spins of the wheel/iterations.</p> <h1 id="the-on-policy-strategy">The On-Policy Strategy</h1> <p>The first way in which we’ll solve this problem is using on-policy Monte Carlo control. Here we will use the ϵ-greedy approach to update the policy function.</p> <h2 id="the-simulation">The Simulation</h2> <p>Let’s first start by defining the episode simulating function using ϵ-greedy.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">run_eps_episode</span><span class="p">(</span><span class="n">env</span><span class="p">,</span> <span class="n">Q</span><span class="p">,</span> <span class="n">epsilon</span><span class="p">,</span> <span class="n">num_actions</span><span class="p">):</span>
    <span class="n">state</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">reset</span><span class="p">()</span>
    <span class="n">memo</span> <span class="o">=</span> <span class="p">[]</span>
    <span class="n">is_done</span> <span class="o">=</span> <span class="bp">False</span>
    <span class="c1"># epsilon-greedy starting probabilities
</span>    <span class="n">proba</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">ones</span><span class="p">(</span><span class="n">num_actions</span><span class="p">)</span> <span class="o">*</span> <span class="n">epsilon</span> <span class="o">/</span> <span class="n">num_actions</span>    
    <span class="k">while</span> <span class="ow">not</span> <span class="n">is_done</span><span class="p">:</span>
        <span class="n">best_action</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">argmax</span><span class="p">(</span><span class="n">Q</span><span class="p">[</span><span class="n">state</span><span class="p">]).</span><span class="n">item</span><span class="p">()</span>
        <span class="n">proba</span><span class="p">[</span><span class="n">best_action</span><span class="p">]</span> <span class="o">+=</span> <span class="mi">1</span> <span class="o">-</span> <span class="n">epsilon</span>
        <span class="c1"># Choose 1 action from the multinomial distribution
</span>        <span class="n">action</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">multinomial</span><span class="p">(</span><span class="n">proba</span><span class="p">,</span> <span class="mi">1</span><span class="p">).</span><span class="n">item</span><span class="p">()</span>               
        
        <span class="n">new_state</span><span class="p">,</span> <span class="n">reward</span><span class="p">,</span> <span class="n">is_done</span><span class="p">,</span> <span class="n">info</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">step</span><span class="p">(</span><span class="n">action</span><span class="p">)</span>
        <span class="n">memo</span><span class="p">.</span><span class="n">append</span><span class="p">((</span><span class="n">state</span><span class="p">,</span> <span class="n">action</span><span class="p">,</span> <span class="n">reward</span><span class="p">))</span>
        <span class="n">state</span> <span class="o">=</span> <span class="n">new_state</span>
    <span class="k">return</span> <span class="n">memo</span>

</code></pre></div></div> <p>The initial probabilities in the ϵ-greedy policy is ϵ/size of the action space: <code class="language-plaintext highlighter-rouge">proba = torch.ones(num_actions) * epsilon / num_actions</code>.</p> <p>Every iteration we update the likelihood of the best action by 1-ϵ: <code class="language-plaintext highlighter-rouge">proba[best_action] += 1 - epsilon</code>. The best action is selected from a multinomial distribution (a distribution with many outcomes) using <code class="language-plaintext highlighter-rouge">torch.multinomial(proba, 1)</code>.</p> <p>Here we are storing the states, actions, and rewards as a list of triples <code class="language-plaintext highlighter-rouge">(state, action, reward)</code>. This makes iterating through them cleaner.</p> <h2 id="the-algorithm">The Algorithm</h2> <p>We use first-visit Monte Carlo to evaluate state-action pairs returned from each simulation.</p> <p>We will use a new data type, the <code class="language-plaintext highlighter-rouge">defaultdict</code> to keep track of rewards, frequency and the Q-function. <code class="language-plaintext highlighter-rouge">defaultdict</code> is a part of the built-in <code class="language-plaintext highlighter-rouge">collections</code> library.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="nn">collections</span> <span class="kn">import</span> <span class="n">defaultdict</span>
</code></pre></div></div> <p>We have to assign a datatype or structure to <code class="language-plaintext highlighter-rouge">defaultdict</code> declarations. This is the default value for any key. For example,</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">d1</span> <span class="o">=</span> <span class="n">defaultdict</span><span class="p">(</span><span class="nb">int</span><span class="p">)</span>
<span class="k">print</span><span class="p">(</span><span class="n">d1</span><span class="p">[</span><span class="mi">5</span><span class="p">])</span>
<span class="o">&gt;&gt;&gt;</span> <span class="mi">0</span>
</code></pre></div></div> <p>If the key does not exist, it is automatically set to the default value. Similarly,</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">d2</span> <span class="o">=</span> <span class="n">defaultdict</span><span class="p">(</span><span class="k">lambda</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">zeros</span><span class="p">(</span><span class="n">num_actions</span><span class="p">))</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">lambda</code> is an inline function. Here a tensor of zeroes is created for each key. Another function we use here is <code class="language-plaintext highlighter-rouge">tqdm</code>. This is a very pleasant wrapper that I highly recommend. Wrap it around a function or a loop and you can see a real-time progress bar.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="nn">tqdm</span> <span class="kn">import</span> <span class="n">tqdm</span>
</code></pre></div></div> <p>If you don’t already have the library, use <code class="language-plaintext highlighter-rouge">pip install tqdm</code> in the terminal. We will see how <code class="language-plaintext highlighter-rouge">tqdm</code> works below.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">on_policy_mc_control</span><span class="p">(</span><span class="n">env</span><span class="p">,</span> <span class="n">gamma</span><span class="p">,</span> <span class="n">epsilon</span><span class="p">,</span> <span class="n">num_episodes</span><span class="p">):</span>
    <span class="n">num_actions</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">action_space</span><span class="p">.</span><span class="n">n</span>
    <span class="n">G_sum</span> <span class="o">=</span> <span class="n">defaultdict</span><span class="p">(</span><span class="nb">float</span><span class="p">)</span>
    <span class="n">N</span> <span class="o">=</span> <span class="n">defaultdict</span><span class="p">(</span><span class="nb">int</span><span class="p">)</span>
    <span class="n">Q</span> <span class="o">=</span> <span class="n">defaultdict</span><span class="p">(</span><span class="k">lambda</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">zeros</span><span class="p">(</span><span class="n">num_actions</span><span class="p">))</span> 
    <span class="k">for</span> <span class="n">episode</span> <span class="ow">in</span> <span class="n">tqdm</span><span class="p">(</span><span class="nb">range</span><span class="p">(</span><span class="n">num_episodes</span><span class="p">)):</span>
        <span class="n">memory</span> <span class="o">=</span> <span class="n">run_eps_episode</span><span class="p">(</span><span class="n">env</span><span class="p">,</span> <span class="n">Q</span><span class="p">,</span> <span class="n">epsilon</span><span class="p">,</span> <span class="n">num_actions</span><span class="p">)</span>
        <span class="n">net_reward</span> <span class="o">=</span> <span class="mi">0</span>
        <span class="n">G</span> <span class="o">=</span> <span class="p">{}</span>
        <span class="k">for</span> <span class="n">state</span><span class="p">,</span> <span class="n">action</span><span class="p">,</span> <span class="n">reward</span> <span class="ow">in</span> <span class="nb">reversed</span><span class="p">(</span><span class="n">memory</span><span class="p">):</span>
            <span class="n">net_reward</span> <span class="o">=</span> <span class="n">gamma</span> <span class="o">*</span> <span class="n">net_reward</span> <span class="o">+</span> <span class="n">reward</span>
            <span class="n">G</span><span class="p">[(</span><span class="n">state</span><span class="p">,</span> <span class="n">action</span><span class="p">)]</span> <span class="o">=</span> <span class="n">net_reward</span>
        <span class="k">for</span> <span class="n">state_action</span><span class="p">,</span> <span class="n">net_reward</span> <span class="ow">in</span> <span class="n">G</span><span class="p">.</span><span class="n">items</span><span class="p">():</span>
            <span class="n">G_sum</span><span class="p">[(</span><span class="n">state</span><span class="p">,</span> <span class="n">action</span><span class="p">)]</span> <span class="o">+=</span> <span class="n">net_reward</span>
            <span class="n">N</span><span class="p">[(</span><span class="n">state</span><span class="p">,</span> <span class="n">action</span><span class="p">)]</span> <span class="o">+=</span> <span class="mi">1</span>
            <span class="n">Q</span><span class="p">[</span><span class="n">state</span><span class="p">][</span><span class="n">action</span><span class="p">]</span> <span class="o">=</span> <span class="n">G_sum</span><span class="p">[(</span><span class="n">state</span><span class="p">,</span> <span class="n">action</span><span class="p">)]</span> <span class="o">/</span> <span class="n">N</span><span class="p">[(</span><span class="n">state</span><span class="p">,</span> <span class="n">action</span><span class="p">)]</span>
    <span class="n">policy</span> <span class="o">=</span> <span class="p">{}</span>
    <span class="k">for</span> <span class="n">state</span><span class="p">,</span> <span class="n">actions</span> <span class="ow">in</span> <span class="n">Q</span><span class="p">.</span><span class="n">items</span><span class="p">():</span>
        <span class="n">policy</span><span class="p">[</span><span class="n">state</span><span class="p">]</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">argmax</span><span class="p">(</span><span class="n">actions</span><span class="p">).</span><span class="n">item</span><span class="p">()</span>
    <span class="k">return</span> <span class="n">Q</span><span class="p">,</span> <span class="n">policy</span>
</code></pre></div></div> <p>We first define our dictionaries</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">G_sum</span> <span class="o">=</span> <span class="n">defaultdict</span><span class="p">(</span><span class="nb">float</span><span class="p">)</span>
<span class="n">N</span> <span class="o">=</span> <span class="n">defaultdict</span><span class="p">(</span><span class="nb">int</span><span class="p">)</span>
<span class="n">Q</span> <span class="o">=</span> <span class="n">defaultdict</span><span class="p">(</span><span class="k">lambda</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">zeros</span><span class="p">(</span><span class="n">num_actions</span><span class="p">))</span> 
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">G_sum</code> records the total rewards through all episodes. <code class="language-plaintext highlighter-rouge">N</code> keeps track of the frequency of each state-action pair. A state-action pair is simply the tuple <code class="language-plaintext highlighter-rouge">(state, action)</code>. <code class="language-plaintext highlighter-rouge">Q</code> is the Q-function, the function that keeps track of the value of state-action pairs.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="n">episode</span> <span class="ow">in</span> <span class="n">tqdm</span><span class="p">(</span><span class="nb">range</span><span class="p">(</span><span class="n">num_episodes</span><span class="p">)):</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">tqdm</code> shows a progress bar and tells you how long it will take for execution to complete.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">memory</span> <span class="o">=</span> <span class="n">run_eps_episode</span><span class="p">(</span><span class="n">env</span><span class="p">,</span> <span class="n">Q</span><span class="p">,</span> <span class="n">epsilon</span><span class="p">,</span> <span class="n">num_actions</span><span class="p">)</span>
<span class="n">net_reward</span> <span class="o">=</span> <span class="mi">0</span>
<span class="n">G</span> <span class="o">=</span> <span class="p">{}</span>
<span class="k">for</span> <span class="n">state</span><span class="p">,</span> <span class="n">action</span><span class="p">,</span> <span class="n">reward</span> <span class="ow">in</span> <span class="nb">reversed</span><span class="p">(</span><span class="n">memory</span><span class="p">):</span>
    <span class="n">net_reward</span> <span class="o">=</span> <span class="n">gamma</span> <span class="o">*</span> <span class="n">net_reward</span> <span class="o">+</span> <span class="n">reward</span>
    <span class="n">G</span><span class="p">[(</span><span class="n">state</span><span class="p">,</span> <span class="n">action</span><span class="p">)]</span> <span class="o">=</span> <span class="n">net_reward</span>
</code></pre></div></div> <p>We initially set the net reward to 0. This is the discounted reward. At each <code class="language-plaintext highlighter-rouge">(state, action, reward)</code> tuple, the net reward is updated and stored in <code class="language-plaintext highlighter-rouge">G</code>.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="n">state_action</span><span class="p">,</span> <span class="n">net_reward</span> <span class="ow">in</span> <span class="n">G</span><span class="p">.</span><span class="n">items</span><span class="p">():</span>
    <span class="n">G_sum</span><span class="p">[(</span><span class="n">state</span><span class="p">,</span> <span class="n">action</span><span class="p">)]</span> <span class="o">+=</span> <span class="n">net_reward</span>
    <span class="n">N</span><span class="p">[(</span><span class="n">state</span><span class="p">,</span> <span class="n">action</span><span class="p">)]</span> <span class="o">+=</span> <span class="mi">1</span>
    <span class="n">Q</span><span class="p">[</span><span class="n">state</span><span class="p">][</span><span class="n">action</span><span class="p">]</span> <span class="o">=</span> <span class="n">G_sum</span><span class="p">[(</span><span class="n">state</span><span class="p">,</span> <span class="n">action</span><span class="p">)]</span> <span class="o">/</span> <span class="n">N</span><span class="p">[(</span><span class="n">state</span><span class="p">,</span> <span class="n">action</span><span class="p">)]</span>
</code></pre></div></div> <p>At the end of each episode, we add the values in <code class="language-plaintext highlighter-rouge">G</code> to <code class="language-plaintext highlighter-rouge">G_sum</code> and increment the frequency counter <code class="language-plaintext highlighter-rouge">N</code> by 1 (first-visit MC). We then update the Q-function to the new average.</p> <p>The benefit of using <code class="language-plaintext highlighter-rouge">defaultdict</code> is that we don’t need to check if a key initially exists. The code simplifies from</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="p">(</span><span class="n">state</span><span class="p">,</span> <span class="n">action</span><span class="p">)</span> <span class="ow">in</span> <span class="n">N</span><span class="p">.</span><span class="n">keys</span><span class="p">():</span>
    <span class="n">N</span><span class="p">[(</span><span class="n">state</span><span class="p">,</span> <span class="n">action</span><span class="p">)]</span> <span class="o">+=</span> <span class="mi">1</span>
<span class="k">else</span><span class="p">:</span>
    <span class="n">N</span><span class="p">[(</span><span class="n">state</span><span class="p">,</span> <span class="n">action</span><span class="p">)]</span> <span class="o">=</span> <span class="mi">1</span>
</code></pre></div></div> <p>to</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">N</span><span class="p">[(</span><span class="n">state</span><span class="p">,</span> <span class="n">action</span><span class="p">)]</span> <span class="o">+=</span> <span class="mi">1</span>
</code></pre></div></div> <p>After numerous sample episodes, we approximate the optimal Q function, which in turn translates to the optimal policy.</p> <h2 id="the-results">The Results</h2> <p>Let’s first set our constants. We use a γ=1 because every reward equally affects the net reward, discounting does not make sense.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">gamma</span> <span class="o">=</span> <span class="mi">1</span>
<span class="n">epsilon</span> <span class="o">=</span> <span class="mf">0.1</span>
<span class="n">num_episodes</span> <span class="o">=</span> <span class="mi">5000</span>
</code></pre></div></div> <p>We can now see the optimal policy for roulette using on-policy MC.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">optimal_Q</span><span class="p">,</span> <span class="n">optimal_policy</span> <span class="o">=</span> <span class="n">on_policy_mc_control</span><span class="p">(</span><span class="n">env</span><span class="p">,</span> <span class="n">gamma</span><span class="p">,</span> <span class="n">epsilon</span><span class="p">,</span> <span class="n">num_episodes</span><span class="p">)</span>
<span class="k">print</span><span class="p">(</span><span class="n">optimal_policy</span><span class="p">)</span>
<span class="o">&gt;&gt;&gt;</span> <span class="p">{</span><span class="mi">0</span><span class="p">:</span> <span class="mi">37</span><span class="p">}</span>
</code></pre></div></div> <p>So the optimal policy is always betting on 37. But wait, 37 maps to walking away from the table. So the best way to win at Roulette is just not playing at all?</p> <p>Let’s now look at how results change when the number of episodes is increased.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>episode_list = [5000, 50000, 500000]

on_policy_dict = {}
for num_episodes in episode_list:
    optimal_Q, optimal_policy = on_policy_mc_control(env, gamma, epsilon, num_episodes)
    on_policy_dict[num_episodes] = [optimal_Q, optimal_policy]
</code></pre></div></div> <p>In each case the optimal policy is still 37. To test our policy, we need to run it in a number of simulations.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">run_test</span><span class="p">(</span><span class="n">policy</span><span class="p">,</span> <span class="n">iters</span><span class="p">,</span> <span class="n">num_episodes</span><span class="p">):</span>
    <span class="n">env</span> <span class="o">=</span> <span class="n">gym</span><span class="p">.</span><span class="n">make</span><span class="p">(</span><span class="s">"Roulette-v0"</span><span class="p">)</span>
    <span class="n">rewards</span> <span class="o">=</span> <span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">*</span> <span class="n">num_episodes</span>
    <span class="n">wins</span> <span class="o">=</span> <span class="mi">0</span>
    <span class="n">losses</span> <span class="o">=</span> <span class="mi">0</span>
    <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="n">num_episodes</span><span class="p">):</span>
        <span class="n">state</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">reset</span><span class="p">()</span>
        <span class="n">is_done</span> <span class="o">=</span> <span class="bp">False</span>
        <span class="n">total_reward</span> <span class="o">=</span> <span class="mi">0</span>
        <span class="k">while</span> <span class="ow">not</span> <span class="n">is_done</span><span class="p">:</span>
            <span class="n">action</span> <span class="o">=</span> <span class="n">policy</span><span class="p">[</span><span class="n">state</span><span class="p">]</span>
            <span class="n">state</span><span class="p">,</span> <span class="n">reward</span><span class="p">,</span> <span class="n">is_done</span><span class="p">,</span> <span class="n">_</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">step</span><span class="p">(</span><span class="n">action</span><span class="p">)</span>
            <span class="n">total_reward</span> <span class="o">+=</span> <span class="n">reward</span>
        <span class="n">rewards</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span> <span class="n">total_reward</span>
        <span class="k">if</span> <span class="n">total_reward</span> <span class="o">&gt;=</span> <span class="mi">1</span><span class="p">:</span>
            <span class="n">wins</span> <span class="o">+=</span> <span class="mi">1</span>
        <span class="k">elif</span> <span class="n">total_reward</span> <span class="o">&lt;=</span> <span class="o">-</span><span class="mi">1</span><span class="p">:</span>
            <span class="n">losses</span> <span class="o">+=</span> <span class="mi">1</span>
    <span class="n">label</span><span class="o">=</span> <span class="sa">f</span><span class="s">"</span><span class="si">{</span><span class="n">iters</span><span class="si">}</span><span class="s"> iterations Win rate: </span><span class="si">{</span><span class="n">wins</span><span class="o">/</span><span class="n">num_episodes</span><span class="si">}</span><span class="s">  Loss rate: </span><span class="si">{</span><span class="n">losses</span><span class="o">/</span><span class="n">num_episodes</span><span class="si">}</span><span class="s">"</span>
    <span class="k">return</span> <span class="n">rewards</span><span class="p">,</span> <span class="n">label</span>
</code></pre></div></div> <p>If the reward is 0 (walking away immediately), it counted as neither a win or a loss. We use the <code class="language-plaintext highlighter-rouge">rewards</code> array and <code class="language-plaintext highlighter-rouge">label</code> in the graphs below.</p> <p>To make visual plots, we use the <code class="language-plaintext highlighter-rouge">matplotlib</code> library.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">matplotlib.pyplot</span> <span class="k">as</span> <span class="n">plt</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">matplotlib</code> is a very user-friendly library, and <a href="https://matplotlib.org/">the website</a> provides great tutorials on usage. For now, we will use the <code class="language-plaintext highlighter-rouge">plot_roulette</code> function for visualizations.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">plot_roulette</span><span class="p">(</span><span class="n">result_dict</span><span class="p">,</span> <span class="n">title</span><span class="p">,</span> <span class="n">num_episodes</span> <span class="o">=</span> <span class="mi">1000</span><span class="p">):</span>
    <span class="k">for</span> <span class="n">k</span><span class="p">,</span> <span class="n">v</span> <span class="ow">in</span> <span class="n">result_dict</span><span class="p">.</span><span class="n">items</span><span class="p">():</span>
        <span class="n">optimal_Q</span><span class="p">,</span> <span class="n">optimal_policy</span> <span class="o">=</span> <span class="n">v</span>
        <span class="n">plt</span><span class="p">.</span><span class="n">plot</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="n">arange</span><span class="p">(</span><span class="nb">len</span><span class="p">(</span><span class="n">optimal_Q</span><span class="p">[</span><span class="mi">0</span><span class="p">])),</span> <span class="n">optimal_Q</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">numpy</span><span class="p">(),</span> <span class="n">label</span><span class="o">=</span><span class="sa">f</span><span class="s">"</span><span class="si">{</span><span class="n">k</span><span class="si">}</span><span class="s"> iterations"</span><span class="p">)</span>
    <span class="n">plt</span><span class="p">.</span><span class="n">xlabel</span><span class="p">(</span><span class="s">"Action"</span><span class="p">)</span>
    <span class="n">plt</span><span class="p">.</span><span class="n">ylabel</span><span class="p">(</span><span class="s">"Action-Value"</span><span class="p">)</span>
    <span class="n">plt</span><span class="p">.</span><span class="n">legend</span><span class="p">()</span>
    <span class="n">plt</span><span class="p">.</span><span class="n">title</span><span class="p">(</span><span class="nb">str</span><span class="p">.</span><span class="n">title</span><span class="p">(</span><span class="n">title</span><span class="p">))</span>
    <span class="n">plt</span><span class="p">.</span><span class="n">show</span><span class="p">()</span>
    <span class="k">for</span> <span class="n">k</span><span class="p">,</span> <span class="n">v</span> <span class="ow">in</span> <span class="n">result_dict</span><span class="p">.</span><span class="n">items</span><span class="p">():</span>
        <span class="n">optimal_Q</span><span class="p">,</span> <span class="n">optimal_policy</span> <span class="o">=</span> <span class="n">v</span>
        <span class="n">rewards</span><span class="p">,</span> <span class="n">label</span> <span class="o">=</span> <span class="n">run_test</span><span class="p">(</span><span class="n">optimal_policy</span><span class="p">,</span> <span class="n">k</span><span class="p">,</span> <span class="n">num_episodes</span><span class="p">)</span>
        <span class="n">plt</span><span class="p">.</span><span class="n">plot</span><span class="p">(</span><span class="n">rewards</span><span class="p">,</span> <span class="n">label</span><span class="o">=</span><span class="n">label</span><span class="p">)</span>
    <span class="n">plt</span><span class="p">.</span><span class="n">xlabel</span><span class="p">(</span><span class="s">"Episode"</span><span class="p">)</span>
    <span class="n">plt</span><span class="p">.</span><span class="n">ylabel</span><span class="p">(</span><span class="s">"Reward"</span><span class="p">)</span>
    <span class="n">plt</span><span class="p">.</span><span class="n">legend</span><span class="p">()</span>
    <span class="n">plt</span><span class="p">.</span><span class="n">show</span><span class="p">()</span>
</code></pre></div></div> <p>From the on-policy MC results, we see the following.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">plot_roulette</span><span class="p">(</span><span class="n">on_policy_dict</span><span class="p">,</span> <span class="s">"on-policy control"</span><span class="p">)</span>
</code></pre></div></div> <figure> <img src="/images/rl/on-policy_control_action_value.png" desc="On-policy roulette action values"/> <figcaption>Action values of on-policy control for different numbers of iterations. The action values converge as the number of iterations increase.</figcaption> </figure> <figure> <img src="/images/rl/on-policy_control_reward.png" desc="On-policy roulette rewards"/> <figcaption>Total reward from running 1000 iterations of the optimal policy. Notice that the reward is always 0.</figcaption> </figure> <p>The action-values meet our expectations here. As the number of episodes increase, the action-values converge to -1/37. The action-value for leaving the board is always 0. As for the total rewards, the agent chooses not to play the game.</p> <h1 id="the-off-policy-strategy">The Off-Policy Strategy</h1> <p>In off-policy MC control, we use a <em>behavior policy</em> to optimize the target policy π. As for the type of importance sampling, we will use the <em>incremental variant</em> of weighted importance sampling. The incremental variant uses a slightly modified update rule speeding up the process significantly. </p> <h2 id="the-simulation-1">The Simulation</h2> <p>For the off-policy simulations, we will follow the random behavior policy to select actions.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">generate_random_policy</span><span class="p">(</span><span class="n">num_actions</span><span class="p">):</span>
    <span class="n">probs</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">ones</span><span class="p">(</span><span class="n">num_actions</span><span class="p">)</span> <span class="o">/</span> <span class="n">num_actions</span>
    <span class="k">def</span> <span class="nf">policy_function</span><span class="p">(</span><span class="n">state</span><span class="p">):</span>
        <span class="k">return</span> <span class="n">probs</span>
    <span class="k">return</span> <span class="n">policy_function</span>
</code></pre></div></div> <p>The behavior policy used is very straightforward, giving each action in any state an equal likelihood of being selected. This ensures stochasticity. Moreover, we usually do not know the number of possible states. Each state getting the same probabilities therefore makes sense.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">run_episode</span><span class="p">(</span><span class="n">env</span><span class="p">,</span> <span class="n">behaviour_policy</span><span class="p">):</span>
    <span class="n">state</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">reset</span><span class="p">()</span>
    <span class="n">memo</span><span class="p">,</span> <span class="n">is_done</span> <span class="o">=</span> <span class="p">[],</span> <span class="bp">False</span>
    <span class="k">while</span> <span class="ow">not</span> <span class="n">is_done</span><span class="p">:</span>
        <span class="n">probs</span> <span class="o">=</span> <span class="n">behaviour_policy</span><span class="p">(</span><span class="n">state</span><span class="p">)</span>
        <span class="n">action</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">multinomial</span><span class="p">(</span><span class="n">probs</span><span class="p">,</span> <span class="mi">1</span><span class="p">).</span><span class="n">item</span><span class="p">()</span>
        <span class="n">new_state</span><span class="p">,</span> <span class="n">reward</span><span class="p">,</span> <span class="n">is_done</span><span class="p">,</span> <span class="n">info</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">step</span><span class="p">(</span><span class="n">action</span><span class="p">)</span>
        <span class="n">memo</span><span class="p">.</span><span class="n">append</span><span class="p">((</span><span class="n">state</span><span class="p">,</span> <span class="n">action</span><span class="p">,</span> <span class="n">reward</span><span class="p">))</span>
        <span class="n">state</span> <span class="o">=</span> <span class="n">new_state</span>
    <span class="k">return</span> <span class="n">memo</span>
</code></pre></div></div> <p>Notice that we use <code class="language-plaintext highlighter-rouge">behaviour_policy(state)</code> to get a 1D Tensor of probabilities. <code class="language-plaintext highlighter-rouge">torch.multinomial(probs, 1)</code> is used to select an action based on the probabilities given.</p> <h2 id="the-algorithm-1">The Algorithm</h2> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">off_policy_mc_contro</span><span class="p">(</span><span class="n">env</span><span class="p">,</span> <span class="n">gamma</span><span class="p">,</span> <span class="n">num_episodes</span><span class="p">,</span> <span class="n">behaviour_policy</span><span class="p">):</span>
    <span class="n">num_actions</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">action_space</span><span class="p">.</span><span class="n">n</span>
    <span class="n">N</span> <span class="o">=</span> <span class="n">defaultdict</span><span class="p">(</span><span class="nb">int</span><span class="p">)</span>
    <span class="n">Q</span> <span class="o">=</span> <span class="n">defaultdict</span><span class="p">(</span><span class="k">lambda</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">zeros</span><span class="p">(</span><span class="n">num_actions</span><span class="p">))</span>
    <span class="k">for</span> <span class="n">episode</span> <span class="ow">in</span> <span class="n">tqdm</span><span class="p">(</span><span class="nb">range</span><span class="p">(</span><span class="n">num_episodes</span><span class="p">)):</span>
        <span class="n">w</span> <span class="o">=</span> <span class="mf">1.0</span>
        <span class="n">states</span><span class="p">,</span> <span class="n">actions</span><span class="p">,</span> <span class="n">rewards</span> <span class="o">=</span> <span class="n">run_episode</span><span class="p">(</span><span class="n">env</span><span class="p">,</span> <span class="n">behaviour_policy</span><span class="p">)</span>
        <span class="n">net_reward</span> <span class="o">=</span> <span class="mi">0</span>    
        <span class="k">for</span> <span class="n">state</span><span class="p">,</span> <span class="n">action</span><span class="p">,</span> <span class="n">reward</span> <span class="ow">in</span> <span class="nb">zip</span><span class="p">(</span><span class="nb">reversed</span><span class="p">(</span><span class="n">states</span><span class="p">),</span> <span class="nb">reversed</span><span class="p">(</span><span class="n">actions</span><span class="p">),</span> <span class="nb">reversed</span><span class="p">(</span><span class="n">rewards</span><span class="p">)):</span>
            <span class="n">net_reward</span> <span class="o">=</span> <span class="n">gamma</span> <span class="o">*</span> <span class="n">net_reward</span> <span class="o">+</span> <span class="n">reward</span>
            <span class="n">N</span><span class="p">[(</span><span class="n">state</span><span class="p">,</span> <span class="n">action</span><span class="p">)]</span> <span class="o">+=</span> <span class="n">w</span>                                <span class="c1"># Incremental update
</span>            <span class="n">Q</span><span class="p">[</span><span class="n">state</span><span class="p">][</span><span class="n">action</span><span class="p">]</span> <span class="o">+=</span> <span class="p">(</span><span class="n">w</span> <span class="o">/</span> <span class="n">N</span><span class="p">[(</span><span class="n">state</span><span class="p">,</span> <span class="n">action</span><span class="p">)])</span> <span class="o">*</span> <span class="p">(</span><span class="n">net_reward</span> <span class="o">-</span> <span class="n">Q</span><span class="p">[</span><span class="n">state</span><span class="p">][</span><span class="n">action</span><span class="p">])</span>   
            <span class="k">if</span> <span class="n">action</span> <span class="o">!=</span> <span class="n">torch</span><span class="p">.</span><span class="n">argmax</span><span class="p">(</span><span class="n">Q</span><span class="p">[</span><span class="n">state</span><span class="p">]).</span><span class="n">item</span><span class="p">():</span>
                <span class="k">break</span>
            <span class="n">w</span> <span class="o">=</span> <span class="n">w</span> <span class="o">/</span> <span class="n">behaviour_policy</span><span class="p">(</span><span class="n">state</span><span class="p">)[</span><span class="n">action</span><span class="p">]</span>
    <span class="n">policy</span> <span class="o">=</span> <span class="p">{}</span>
    <span class="k">for</span> <span class="n">state</span><span class="p">,</span> <span class="n">actions</span> <span class="ow">in</span> <span class="n">Q</span><span class="p">.</span><span class="n">items</span><span class="p">():</span>
        <span class="n">policy</span><span class="p">[</span><span class="n">state</span><span class="p">]</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">argmax</span><span class="p">(</span><span class="n">actions</span><span class="p">).</span><span class="n">item</span><span class="p">()</span>
    <span class="k">return</span> <span class="n">Q</span><span class="p">,</span> <span class="n">policy</span>
</code></pre></div></div> <p>Due to the incremental update, we do not keep track of rewards, <code class="language-plaintext highlighter-rouge">G_sum</code>, instead updating the Q-function in-place. We substitute <img src="https://render.githubusercontent.com/render/math?math=V_n=\sum_{k=1}^{n}(w_kR_k)/\sum_{k=1}^{n}w_k"/> with <img src="https://render.githubusercontent.com/render/math?math=v_{n %2B 1}=V_n %2B w_{n %2B 1 }(R_{n %2B 1} - V_n)/N_{n %2B 1}"/>. Instead of calculating the Q-function in a for loop in the end, we update it in each iteration. <code class="language-plaintext highlighter-rouge">N</code> is the sum of weights for the state-action pair up-to that point.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">N</span><span class="p">[(</span><span class="n">state</span><span class="p">,</span> <span class="n">action</span><span class="p">)]</span> <span class="o">+=</span> <span class="n">w</span>
<span class="n">Q</span><span class="p">[</span><span class="n">state</span><span class="p">][</span><span class="n">action</span><span class="p">]</span> <span class="o">+=</span> <span class="p">(</span><span class="n">w</span> <span class="o">/</span> <span class="n">N</span><span class="p">[(</span><span class="n">state</span><span class="p">,</span> <span class="n">action</span><span class="p">)])</span> <span class="o">*</span> <span class="p">(</span><span class="n">net_reward</span> <span class="o">-</span> <span class="n">Q</span><span class="p">[</span><span class="n">state</span><span class="p">][</span><span class="n">action</span><span class="p">])</span> 
</code></pre></div></div> <p>This time, we need to keep track of a new variable, <code class="language-plaintext highlighter-rouge">w</code>, the weight of each state-action pair. As the number of appearances of a state-action pair increases, <code class="language-plaintext highlighter-rouge">w</code> increases. This is because the denominator<br/> <code class="language-plaintext highlighter-rouge">behaviour_policy(state)[action]</code><img src="https://render.githubusercontent.com/render/math?math=\leq 1"/> . States that appear earlier on are weighed higher.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="n">action</span> <span class="o">!=</span> <span class="n">torch</span><span class="p">.</span><span class="n">argmax</span><span class="p">(</span><span class="n">Q</span><span class="p">[</span><span class="n">state</span><span class="p">]):</span>
    <span class="k">break</span>
</code></pre></div></div> <p>The update process only runs when the target and behavior policies choose the same actions.</p> <h2 id="the-results-1">The Results</h2> <p>We will use the same γ=1 here.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">gamma</span> <span class="o">=</span> <span class="mi">1</span>
<span class="n">episode_list</span> <span class="o">=</span> <span class="p">[</span><span class="mi">5000</span><span class="p">,</span> <span class="mi">50000</span><span class="p">,</span> <span class="mi">500000</span><span class="p">]</span>
<span class="n">random_policy</span> <span class="o">=</span> <span class="n">generate_random_policy</span><span class="p">(</span><span class="n">env</span><span class="p">.</span><span class="n">action_space</span><span class="p">.</span><span class="n">n</span><span class="p">)</span>
</code></pre></div></div> <p>The results this time are awry. As the number of iterations increase, the optimal policy changes, albeit to an incorrect outcome.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">off_policy_dict</span> <span class="o">=</span> <span class="p">{}</span>
<span class="k">for</span> <span class="n">num_episodes</span> <span class="ow">in</span> <span class="n">episode_list</span><span class="p">:</span>
    <span class="n">optimal_Q</span><span class="p">,</span> <span class="n">optimal_policy</span> <span class="o">=</span> <span class="n">off_policy_mc_control</span><span class="p">(</span><span class="n">env</span><span class="p">,</span> <span class="n">gamma</span><span class="p">,</span> <span class="n">num_episodes</span><span class="p">,</span> <span class="n">random_policy</span><span class="p">)</span>
    <span class="n">off_policy_dict</span><span class="p">[</span><span class="n">num_episodes</span><span class="p">]</span> <span class="o">=</span> <span class="p">[</span><span class="n">optimal_Q</span><span class="p">,</span> <span class="n">optimal_policy</span><span class="p">]</span>
    <span class="k">print</span><span class="p">(</span><span class="n">num_episodes</span><span class="p">,</span> <span class="s">": "</span><span class="p">,</span> <span class="n">optimal_policy</span><span class="p">)</span>
<span class="o">&gt;&gt;&gt;</span> <span class="mi">5000</span><span class="p">:</span> <span class="p">{</span><span class="mi">0</span><span class="p">:</span> <span class="mi">25</span><span class="p">}</span>
<span class="o">&gt;&gt;&gt;</span> <span class="mi">50000</span><span class="p">:</span> <span class="p">{</span><span class="mi">0</span><span class="p">:</span> <span class="mi">30</span><span class="p">}</span>
<span class="o">&gt;&gt;&gt;</span> <span class="mi">500000</span><span class="p">:</span> <span class="p">{</span><span class="mi">0</span><span class="p">:</span> <span class="mi">1</span><span class="p">}</span>
</code></pre></div></div> <p>We see a similar mess in the plots.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">plot_roulette</span><span class="p">(</span><span class="n">off_policy_dict</span><span class="p">,</span> <span class="s">"off-policy control"</span><span class="p">,</span> <span class="n">num_episodes</span><span class="o">=</span><span class="mi">1000</span><span class="p">)</span>
</code></pre></div></div> <figure> <img src="/images/rl/off-policy_control_action_value.png" desc="Off-policy roulette action values"/> <figcaption>Action values of off-policy control for different numbers of iterations. Convergence? </figcaption> </figure> <figure> <img src="/images/rl/off-policy_control_reward.png" desc="Off-policy roulette rewards"/> <figcaption>Total reward from running 1000 iterations of the optimal policy. There are massive fluctuations in rewards.</figcaption> </figure> <p>Off-policy Monte Carlo control fails to solve the roulette problem. Running the algorithm gives a different optimal policy each time. My hypothesis is that because off-policy control stops learning from an episode the moment the behavior and target policies differ, whenever the optimal early stopping scenario comes up in the behavior policy sequence, it doesn’t match the target policy. Thus the agent doesn’t learn from that episode. In cases where the first few actions may have a simulation-ending impact, off-policy Monte Carlo control may be unsuitable.</p> <h1 id="conclusion">Conclusion</h1> <p>On-policy and off-policy Monte Carlo control algorithms have their strengths and weaknesses. The weakness of the off-policy approach was seen in roulette. On-policy MC is also significantly faster per episode in this environment, taking 69 seconds for 500,000 iterations. On the other hand, off-policy MC required 40 minutes for the same task.</p> <p>It should be noted that when the agent is not allowed to walk away from the game, both policies converge to the same optimal policy <code class="language-plaintext highlighter-rouge">{0:0}</code> -&gt; the agent always bets on the high-risk, high-reward 0. I’ll leave that for you to try out; one just has to prevent action 37 from being selected. In other games like blackjack, off-policy Monte Carlo fares slightly better than its on-policy counterpart (42.4% vs 43.7% win rate<sup><a href="#myfootnote2">2</a></sup>).</p> <p>So there we have it, Monte Carlo reinforcement learning - a powerful tool. The takeaway from this article - maybe tone down on the little wheel, the only one feeling real lucky is the casino.</p> <h4 id="footnotes">Footnotes</h4> <p><a name="myfootnote1">[1]</a> Master Traditional Games. <a href="https://www.mastersofgames.com/rules/roulette-rules.htm">Rules of Roulette</a></p> <p><a name="myfootnote2">[2]</a> This result is from some experiments I ran following Sutton &amp; Barlo’s Reinforcement Learning, An Introduction. This environment is available in the Gym as <code class="language-plaintext highlighter-rouge">Blackjack-v0</code>.</p>]]></content><author><name></name></author><category term="Reinforcement Learning"/><category term="Monte Carlo"/><category term="MC Control"/><category term="RL Basics"/><category term="RL Games"/><summary type="html"><![CDATA[Previously, we looked into the Monte Carlo family of reinforcement learning algorithms. These methods estimate the solution to a RL problem by learning from experience. Just by simulation and blind exploration, Monte Carlo agent reaches the optimal solution.]]></summary></entry><entry><title type="html">Monte Carlo - Learning from Experience</title><link href="https://alshedivat.github.io/al-folio/blog/rl_mc/" rel="alternate" type="text/html" title="Monte Carlo - Learning from Experience"/><published>2021-06-20T00:00:00+00:00</published><updated>2021-06-20T00:00:00+00:00</updated><id>https://alshedivat.github.io/al-folio/blog/mc</id><content type="html" xml:base="https://alshedivat.github.io/al-folio/blog/rl_mc/"><![CDATA[<p>Last time we looked at dynamic programming, methods that compute near-exact solutions to problems using a full-scale model of the environment dynamics. This time we are doing a complete 180. Monte Carlo(MC) methods are everything DP is not.</p> <p>Monte Carlo methods consist of interacting directly with the environment - <strong>simulated experience</strong>, and then <strong>averaging rewards</strong> through the states the model went through. Monte Carlo methods in general refer to estimation methods where actions are often completely randomized.</p> <ul> <li>Complete knowledge of the environment is unnecessary.</li> <li>Works based on averaging outcomes (more on that later)</li> <li>Easily scalable to larger problems.</li> <li>Estimates are independent, i.e. no bootstrapping</li> <li>Only a single sequence of events is looked into at each iteration (no full backups)</li> </ul> <p>Don’t let this fool you, the methods we have seen in DP are still very important in Monte Carlo. The iterative cycle of evaluation followed by improvement (known as the General Policy Iteration or GPI) seen before is used in MC.</p> <p>When we say that complete environment knowledge is unnecessary or MC methods do not require environment models, we mean that the agent does not have access to the complete environment; it can only interact with the environment and receive rewards.</p> <h2 id="monte-carlo-evaluation">Monte Carlo Evaluation</h2> <p>Monte Carlo methods work by averaging the rewards seen in each episode over the states (or state-action pairs) the agent goes through. There are two ways in which we evaluate states in a Monte Carlo policy:</p> <ul> <li><strong>First-visit MC</strong>: In each episode, for a state <code class="language-plaintext highlighter-rouge">s</code> we only record the first time we receive a reward when state <code class="language-plaintext highlighter-rouge">s</code> was visited as its reward. In a single episode we may end up in state <code class="language-plaintext highlighter-rouge">s</code> multiple times, but only the first such instance is considered.</li> <li><strong>Every-visit MC</strong>: Here we record all the times we end up in state <code class="language-plaintext highlighter-rouge">s</code> in each episode, take separate tallies for each encounter, and average tallied rewards over the number of encounters.</li> </ul> <p>Suppose we are trying to calculate the value of a state X. In the first visit method, after we reach X we start to sum the rewards until the end of the episode. If the state X appears again, ignore it and don’t start counting again. The value of the state X is the average sum for all episodes where X appears.</p> <p>The every visit method works in a similar way, but if the state X reappears in the episode, start another sum (and keep summing the previous sums too). Then average all sums for the value of that state.</p> <table> <tr><th colspan="2">Episode 1 </th><th colspan="2">Episode 2</th></tr> <tr><th>State </th><th>Reward</th><th>State </th><th>Reward</th></tr> <tr> <th> A </th> <th> 2 </th> <th> B </th> <th> 5 </th> </tr> <tr> <th> B </th> <th> 3 </th> <th> B </th> <th> 2 </th> </tr> <tr> <th> A </th> <th> -6 </th> <th> A </th> <th> -1 </th> </tr> <tr> <th> B </th> <th> 8 </th> <th> B </th> <th> 5 </th> </tr> <tr> <th> B </th> <th> -6 </th> <th> B </th> <th> -4 </th> </tr> </table> <p>In first-visit MC, the rewards that appear after A in episode 1 is 2+3-6+8-6=1; in episode 2 it is -1+5-4=0 (Start from the first time the state is observed). V(A) is therefore (1+0)/2=0.5. Similarly V(B)=(-1+7)/2=3.</p> <p>In the implementations of the Monte Carlo policy evaluation functions, we will use a <code class="language-plaintext highlighter-rouge">run_episode</code> method that will return lists of states and rewards.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># First Visit Monte Carlo Policy Evaluation
</span><span class="k">def</span> <span class="nf">mc_eval_first_visit</span><span class="p">(</span><span class="n">env</span><span class="p">,</span> <span class="n">policy</span><span class="p">,</span> <span class="n">gamma</span><span class="p">,</span> <span class="n">num_episodes</span><span class="p">):</span>
    <span class="n">num_states</span> <span class="o">=</span> <span class="n">policy</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span>
    <span class="c1"># Value tensor
</span>    <span class="n">V</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">zeros</span><span class="p">(</span><span class="n">num_states</span><span class="p">)</span>
    <span class="c1"># Number of times a state is counted   
</span>    <span class="c1"># Once for each episode the state appears
</span>    <span class="n">N</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">zeros</span><span class="p">(</span><span class="n">num_states</span><span class="p">)</span>
    <span class="k">for</span> <span class="n">episode</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="n">num_episodes</span><span class="p">):</span>
        <span class="n">states</span><span class="p">,</span> <span class="n">rewards</span> <span class="o">=</span> <span class="n">run_episode</span><span class="p">(</span><span class="n">env</span><span class="p">,</span> <span class="n">policy</span><span class="p">)</span>
        <span class="c1"># Reward tensor
</span>        <span class="n">G</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">zeros</span><span class="p">(</span><span class="n">num_states</span><span class="p">)</span>
        <span class="c1"># flag variable array
</span>        <span class="n">first_visit</span> <span class="o">=</span> <span class="p">[</span><span class="bp">False</span><span class="p">]</span> <span class="o">*</span> <span class="n">num_states</span>
        <span class="n">disc_reward</span> <span class="o">=</span> <span class="mi">0</span> <span class="c1"># Discounted reward
</span>        <span class="k">for</span> <span class="n">state</span><span class="p">,</span> <span class="n">reward</span> <span class="ow">in</span> <span class="nb">zip</span><span class="p">(</span><span class="nb">reversed</span><span class="p">(</span><span class="n">states</span><span class="p">),</span> <span class="nb">reversed</span><span class="p">(</span><span class="n">rewards</span><span class="p">)):</span>
            <span class="n">disc_reward</span> <span class="o">=</span> <span class="n">gamma</span> <span class="o">*</span> <span class="n">disc_reward</span> <span class="o">+</span> <span class="n">reward</span>            
            <span class="n">G</span><span class="p">[</span><span class="n">state</span><span class="p">]</span> <span class="o">=</span> <span class="n">disc_reward</span>
            <span class="n">first_visit</span><span class="p">[</span><span class="n">state</span><span class="p">]</span> <span class="o">=</span> <span class="bp">True</span>
        <span class="c1"># Update value and counter tensors
</span>        <span class="k">for</span> <span class="n">state</span> <span class="ow">in</span> <span class="n">states</span><span class="p">:</span>
            <span class="k">if</span> <span class="n">first_visit</span><span class="p">[</span><span class="n">state</span><span class="p">]:</span>
                <span class="n">V</span><span class="p">[</span><span class="n">state</span><span class="p">]</span> <span class="o">+=</span> <span class="n">G</span><span class="p">[</span><span class="n">state</span><span class="p">]</span>
                <span class="n">N</span><span class="p">[</span><span class="n">state</span><span class="p">]</span> <span class="o">+=</span> <span class="mi">1</span>
    <span class="c1"># Averaging first returns
</span>    <span class="k">for</span> <span class="n">state</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="n">num_states</span><span class="p">):</span>
        <span class="k">if</span> <span class="n">N</span><span class="p">[</span><span class="n">state</span><span class="p">]</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">:</span>
            <span class="n">V</span><span class="p">[</span><span class="n">state</span><span class="p">]</span> <span class="o">=</span> <span class="n">V</span><span class="p">[</span><span class="n">state</span><span class="p">]</span> <span class="o">/</span> <span class="n">N</span><span class="p">[</span><span class="n">state</span><span class="p">]</span>     
    <span class="k">return</span> <span class="n">V</span>
</code></pre></div></div> <p>Let’s break this down. First, we go through the states and rewards in reverse. This is done to properly calculate discounting. The net reward <code class="language-plaintext highlighter-rouge">G</code> for each state is updated and the <code class="language-plaintext highlighter-rouge">first_visit</code> flag is set to <code class="language-plaintext highlighter-rouge">True</code>.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="n">state</span><span class="p">,</span> <span class="n">reward</span> <span class="ow">in</span> <span class="nb">zip</span><span class="p">(</span><span class="nb">reversed</span><span class="p">(</span><span class="n">states</span><span class="p">),</span> <span class="nb">reversed</span><span class="p">(</span><span class="n">rewards</span><span class="p">)):</span>
    <span class="n">disc_reward</span> <span class="o">=</span> <span class="n">gamma</span> <span class="o">*</span> <span class="n">disc_reward</span> <span class="o">+</span> <span class="n">reward</span>            
    <span class="n">G</span><span class="p">[</span><span class="n">state</span><span class="p">]</span> <span class="o">=</span> <span class="n">disc_reward</span>
    <span class="n">first_visit</span><span class="p">[</span><span class="n">state</span><span class="p">]</span> <span class="o">=</span> <span class="bp">True</span>
</code></pre></div></div> <p>After going through the result of an episode, the value and counter tensors are updated. In first-visit MC, the count for a state is updated by at most 1 in any given episode.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="k">for</span> <span class="n">state</span> <span class="ow">in</span> <span class="n">states</span><span class="p">:</span>
    <span class="k">if</span> <span class="n">first_visit</span><span class="p">[</span><span class="n">state</span><span class="p">]:</span>
        <span class="n">V</span><span class="p">[</span><span class="n">state</span><span class="p">]</span> <span class="o">+=</span> <span class="n">G</span><span class="p">[</span><span class="n">state</span><span class="p">]</span>
        <span class="n">N</span><span class="p">[</span><span class="n">state</span><span class="p">]</span> <span class="o">+=</span> <span class="mi">1</span>
</code></pre></div></div> <p>After simulating a number of episodes, the value tensor is divided by the number of appearances of each state to obtain the value function.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="n">state</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="n">num_states</span><span class="p">):</span>
    <span class="k">if</span> <span class="n">N</span><span class="p">[</span><span class="n">state</span><span class="p">]</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">:</span>
        <span class="n">V</span><span class="p">[</span><span class="n">state</span><span class="p">]</span> <span class="o">=</span> <span class="n">V</span><span class="p">[</span><span class="n">state</span><span class="p">]</span> <span class="o">/</span> <span class="n">N</span><span class="p">[</span><span class="n">state</span><span class="p">]</span> 
</code></pre></div></div> <p>In every-visit MC, each time a state appears we start a new tally. A appears in episode 1 twice and once in episode 2. The rewards are 1 for the first tally and -4 for the second tally in episode 1 and 0 in episode 2. V(A)=(1-4+0)/3=-1. State B appears 7 times in total in the 2 episodes. Therefore, V(B)=(-1+2-6+7+2+1-4)/7=1/7 (gamma = 1).</p> <p>The differences in implementation are slight, yet significant.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Every Visit Monte Carlo Policy Evaluation
</span><span class="k">def</span> <span class="nf">mc_eval_every_visit</span><span class="p">(</span><span class="n">env</span><span class="p">,</span> <span class="n">policy</span><span class="p">,</span> <span class="n">gamma</span><span class="p">,</span> <span class="n">num_episodes</span><span class="p">):</span>
    <span class="n">num_states</span> <span class="o">=</span> <span class="n">policy</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span>
    <span class="c1"># Value tensor
</span>    <span class="n">V</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">zeros</span><span class="p">(</span><span class="n">num_states</span><span class="p">)</span>
    <span class="c1"># Number of times a state is counted   
</span>    <span class="c1"># Once for each episode the state appears
</span>    <span class="n">N</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">zeros</span><span class="p">(</span><span class="n">num_states</span><span class="p">)</span>
    <span class="c1"># Reward tensor, is now incremented
</span>    <span class="n">G</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">zeros</span><span class="p">(</span><span class="n">num_states</span><span class="p">)</span>
    <span class="k">for</span> <span class="n">episode</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="n">num_episodes</span><span class="p">):</span>
        <span class="n">states</span><span class="p">,</span> <span class="n">rewards</span> <span class="o">=</span> <span class="n">run_episode</span><span class="p">(</span><span class="n">env</span><span class="p">,</span> <span class="n">policy</span><span class="p">)</span>
        <span class="n">disc_reward</span> <span class="o">=</span> <span class="mi">0</span> <span class="c1"># Discounted reward
</span>        <span class="k">for</span> <span class="n">state</span><span class="p">,</span> <span class="n">reward</span> <span class="ow">in</span> <span class="nb">zip</span><span class="p">(</span><span class="nb">reversed</span><span class="p">(</span><span class="n">states</span><span class="p">),</span> <span class="nb">reversed</span><span class="p">(</span><span class="n">rewards</span><span class="p">)):</span>
            <span class="c1"># Every reward added
</span>            <span class="n">disc_reward</span> <span class="o">=</span> <span class="n">gamma</span> <span class="o">*</span> <span class="n">disc_reward</span> <span class="o">+</span> <span class="n">reward</span>            
            <span class="n">G</span><span class="p">[</span><span class="n">state</span><span class="p">]</span> <span class="o">+=</span> <span class="n">disc_reward</span>
            <span class="n">N</span><span class="p">[</span><span class="n">state</span><span class="p">]</span> <span class="o">+=</span> <span class="mi">1</span>
    <span class="c1"># Averaging total returns
</span>    <span class="k">for</span> <span class="n">state</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="n">num_states</span><span class="p">):</span>
        <span class="k">if</span> <span class="n">N</span><span class="p">[</span><span class="n">state</span><span class="p">]</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">:</span>
            <span class="n">V</span><span class="p">[</span><span class="n">state</span><span class="p">]</span> <span class="o">=</span> <span class="n">G</span><span class="p">[</span><span class="n">state</span><span class="p">]</span> <span class="o">/</span> <span class="n">N</span><span class="p">[</span><span class="n">state</span><span class="p">]</span>     
    <span class="k">return</span> <span class="n">V</span>
</code></pre></div></div> <p>The differences in implementation are:</p> <ul> <li>The reward tensor <code class="language-plaintext highlighter-rouge">G</code> is no longer reset after each episode. Instead it keeps a total tally.</li> <li>No flag variables used. Instead <code class="language-plaintext highlighter-rouge">N</code> is updated at each state visited.</li> <li><code class="language-plaintext highlighter-rouge">V[state] = V[state] / N[state]</code> is replaced by <code class="language-plaintext highlighter-rouge">V[state] = G[state] / N[state]</code> when averaging. This is done because <code class="language-plaintext highlighter-rouge">G</code> now keeps track of all rewards.</li> </ul> <p>After every episode we only update the states we visited (in contrast to DP where everything is updated). First-visit and every-visit MC defines ways to score states and state-action pairs. These score estimates (akin to policy evaluation) are used to approximate an optimal policy (policy improvement). To achieve this policy, we need to explore the environment and evaluate all possible states the agent can end up in during an episode. Since, unlike DP, MC only updates the states it experiences in an episode, exploration has a greater emphasis here. Moreover, we don’t even know what all the possible states in the environment are (since we don’t have a model for the environment to access).</p> <p>The best way to decide between a first-visit and every-visit strategy is implementation. Oftentimes the nature of the task may indicate which strategy is more appropriate; it is still good practice to try out both methods when unsure.</p> <h2 id="monte-carlo-control-a-prologue">Monte Carlo Control: A Prologue</h2> <p>When an environment model is unavailable, we cannot estimate state values (since we don’t know how likely it is for us to come to a particular state, i.e., probabilities). We can only estimate the value of actions by interacting with the environment.</p> <p>This poses a problem: if we let the agent greedily take actions or always follow the same policy, the agent won’t explore the other options. Actions that may seem poor at first may be much better down the line. A fully greedy approach won’t do.</p> <h3 id="exploring-starts">Exploring Starts</h3> <p>A possible solution here is what’s known as <strong>exploring starts</strong>. Here we choose our first action randomly. This is under the assumption that every following state-action pair has some chance of taking place. Of course this assumption doesn’t actually hold, but exploring starts is very easy to implement.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># For the very first action
</span><span class="n">action</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">randint</span><span class="p">(</span><span class="n">low</span><span class="o">=</span><span class="mi">0</span><span class="p">,</span><span class="n">high</span><span class="o">=</span><span class="n">num_actions</span><span class="p">,</span> <span class="n">size</span><span class="o">=</span><span class="p">[</span><span class="mi">1</span><span class="p">]).</span><span class="n">item</span><span class="p">()</span>
</code></pre></div></div> <p>The first action just has to be random. The rest of the actions are selected based on the matrix of action values<sup><a href="#myfootnote1">1</a></sup>.</p> <h3 id="stochastic-policy">Stochastic Policy</h3> <p>A more robust way to ensure exploration is by making exploration a part of the policy. This means that instead of greedily selecting the best action all the time, all actions are given a non-zero likelihood of taking place. While the best action has the highest probability of happening, the agent selects an action from the distribution randomly. This ensures some exploration always takes place.</p> <h1 id="avoiding-exploring-starts-monte-carlo-control">Avoiding Exploring Starts: Monte Carlo Control</h1> <p>Exploring starts is based on an assumption that simply does not hold. Here we look at the ways we can ensure a good Monte-Carlo policy search algorithm that doesn’t rely on exploring starts.</p> <h2 id="on-policy-mc-control">On-policy MC Control</h2> <p>On-policy Monte Carlo control is very similar to policy iteration; the optimal policy is learned by starting with a given policy and slowly improving it over many iterations. Here, instead of updating a value function for the state, we evaluate the state-action pair values. This is a subtlety of Monte Carlo control. These action values are also known as the <strong>Q-function</strong>. In the improvement phase we select the best actions the same way as before, running <code class="language-plaintext highlighter-rouge">torch.argmax()</code> on the Q function for every state seen. Upon repeating this many times, we reach the optimal policy.</p> <p>The initial policy here has to adhere to a strict condition - stochasticity. That is, every action has some likelihood of being selected. Policies that can ensure this are called <em>soft policies</em>. A simple example here would be always selecting a random action at each state. A better policy would be what we call an <strong>ϵ-greedy policy</strong>.</p> <h3 id="ϵ-greedy-a-bit-less-greed">ϵ-Greedy: A bit less greed</h3> <p>The ϵ-greedy policy is an easy policy to understand, but requires a bit of mathematical insight to prove. Of course I will not be going over the proof for the sake of sanity, but feel free to look into it if you have the time.</p> <p>For an ϵ-greedy policy <code class="language-plaintext highlighter-rouge">π</code>, the update rule for a state <code class="language-plaintext highlighter-rouge">s</code> and action <code class="language-plaintext highlighter-rouge">a</code> is as follows</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="n">a</span> <span class="ow">is</span> <span class="n">optimal</span><span class="p">:</span>
    <span class="n">π</span><span class="p">[</span><span class="n">s</span><span class="p">][</span><span class="n">a</span><span class="p">]</span> <span class="o">=</span> <span class="mi">1</span> <span class="o">-</span> <span class="n">eps</span> <span class="o">+</span> <span class="n">eps</span><span class="o">/</span><span class="n">num_actions</span>
<span class="k">else</span><span class="p">:</span>
    <span class="n">π</span><span class="p">[</span><span class="n">s</span><span class="p">][</span><span class="n">a</span><span class="p">]</span> <span class="o">=</span> <span class="n">eps</span><span class="o">/</span><span class="n">num_actions</span>
</code></pre></div></div> <p>where eps(ϵ) is a parameter determining the minimum likelihood of an action of taking place. The higher the ϵ, the more the exploration<sup><a href="#myfootnote2">2</a></sup>. Actions are selected on a probabilistic basis, i.e. the less likely option will be selected sometimes.</p> <h2 id="off-policy-mc-control">Off-policy MC Control</h2> <p>Off-policy MC control uses a different technique to encourage exploration. Here, instead of building randomness into the agent’s policy, we use a separate policy for exploration. The policy that we update after each iteration is called the <strong>target policy, π</strong>, and the policy that is used for exploration is the <strong>behavior policy, b</strong>. But how do we use one policy to train another one?</p> <h3 id="importance-sampling">Importance Sampling</h3> <p>Sampling is a statistical method to take a number of items from a group to estimate the properties of said group. As we are using two different policies here, we only take into account the actions that were taken by both policies, i.e., the <em>common steps</em>. The importance sampling pseudocode is as follows.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">states</span><span class="p">,</span> <span class="n">actions</span><span class="p">,</span> <span class="n">rewards</span> <span class="o">=</span> <span class="n">run_episode</span><span class="p">(</span><span class="n">env</span><span class="p">,</span> <span class="n">behaviour_policy</span><span class="p">)</span>
<span class="n">weight</span> <span class="o">=</span> <span class="mf">1.0</span>
<span class="n">return_g</span> <span class="o">=</span> <span class="mf">0.0</span>  <span class="c1"># Final reward, G
</span><span class="k">for</span> <span class="n">state</span><span class="p">,</span> <span class="n">action</span><span class="p">,</span> <span class="n">reward</span> <span class="ow">in</span> <span class="nb">zip</span><span class="p">(</span><span class="nb">reversed</span><span class="p">(</span><span class="n">states</span><span class="p">),</span> <span class="nb">reversed</span><span class="p">(</span><span class="n">actions</span><span class="p">),</span> <span class="nb">reversed</span><span class="p">(</span><span class="n">rewards</span><span class="p">)):</span>
    <span class="n">return_g</span> <span class="o">=</span> <span class="n">gamma</span> <span class="o">*</span> <span class="n">return_g</span> <span class="o">+</span> <span class="n">reward</span>  <span class="c1"># Discounted reward
</span>    <span class="c1"># N here is a dictionary counting the number of times 
</span>    <span class="c1"># a state-action pair has occured
</span>    <span class="n">N</span><span class="p">[(</span><span class="n">state</span><span class="p">,</span> <span class="n">action</span><span class="p">)]</span> <span class="o">+=</span> <span class="mi">1</span>
    <span class="n">Q</span><span class="p">[</span><span class="n">state</span><span class="p">][</span><span class="n">action</span><span class="p">]</span> <span class="o">+=</span> <span class="p">(</span><span class="n">weight</span><span class="o">/</span><span class="n">N</span><span class="p">[</span><span class="n">state</span><span class="p">,</span> <span class="n">action</span><span class="p">])</span>
    <span class="c1"># Update weight by dividing it by probability of the action taken
</span>    <span class="n">weight</span> <span class="o">=</span> <span class="n">weight</span><span class="o">/</span><span class="n">behaviour_policy</span><span class="p">(</span><span class="n">state</span><span class="p">)[</span><span class="n">action</span><span class="p">]</span>
    <span class="c1"># Stop if behaviour policy's action does not match that of target policy
</span>
    <span class="k">if</span> <span class="n">action</span> <span class="o">!=</span> <span class="n">torch</span><span class="p">.</span><span class="n">argmax</span><span class="p">(</span><span class="n">Q</span><span class="p">[</span><span class="n">state</span><span class="p">]).</span><span class="n">item</span><span class="p">():</span>
        <span class="k">break</span>
</code></pre></div></div> <p>There are two types of importance sampling, ordinary and weighted. The above algorithm describes ordinary importance sampling. We calculate the value function using the equation <img src="https://render.githubusercontent.com/render/math?math=V_n=\sum_{k=1}^{n}(w_kR_k)/n"/></p> <p>Ordinary importance sampling has a significant drawback. Mathematical analysis has shown that in ordinary importance sampling variance can be as high as infinity. This means that this method can fail to converge or take longer to do so. The variance is unbounded because we are simply taking an average. A better alternative is weighted importance sampling <img src="https://render.githubusercontent.com/render/math?math=V_n=\sum_{k=1}^{n}(w_kR_k)/\sum_{k=1}^{n}w_k"/> Here instead of taking a simple average, we take weighted averages of the returns. Since the largest weight(probability of the action taking place) of any single return is 1, the variance of the function converges to 0.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">N</span><span class="p">[(</span><span class="n">state</span><span class="p">,</span> <span class="n">action</span><span class="p">)]</span> <span class="o">+=</span> <span class="n">weight</span>
</code></pre></div></div> <p>Changing this one line converts ordinary importance sampling to weighted importance sampling.</p> <h2 id="conclusion">Conclusion</h2> <p>Monte Carlo methods are versatile, easy to implement, and quick to learn. The primary appeal of these methods is their ability to learn just from interacting with the environment. We estimate the optimal policy and value function by letting the agent gain experience in the form of <em>sample episodes</em>.</p> <p>In many practical scenarios it is much easier to simulate sample episodes than it is to construct exact models that would be required for DP methods. Moreover, since MC only evaluates the states the agent encounters, it does not waste time evaluating the entire state space.</p> <p>A drawback of Monte Carlo methods is that these algorithms only work on episodic tasks. Defining end points for continuing tasks to take averages from is far more challenging, and oftentimes unintuitive.</p> <p>First-visit and every-visit Monte Carlo evaluation are two different ways to assess the relative value of a state or state-action pair. It is difficult to choose one method over the other. Therefore, apply both of them. Most of the time the result will be the same.</p> <p>Monte Carlo methods have to ensure sufficient exploration when looking for the optimal policy. On-policy and off-policy Monte Carlo control are two good methods two perform this search. Again, it may sometimes be the case that the two approaches result in two different answers, in which case it is up to us to determine the best answer. We will see one such example in the next post.</p> <p>In this article we went through the theory and algorithms of Monte Carlo reinforcement learning. Next time we will go through an interesting real-world example: <a href="https://shirsho-12.github.io/blog/rl_mc_roulette/"><strong>Roulette</strong> - To Play or Not to Play</a>?</p> <h4 id="footnotes">Footnotes</h4> <p><a name="myfootnote1">[1]</a> The action-value matrix works identically to a state-value matrix. The way in which we work with action-value matrices is also similar.</p> <p><a name="myfootnote2">[2]</a> The ϵ parameter has a very strong influencing in learning. If it is set too low, the resulting policy may not be the optimal one. On the other hand, if ϵ is too high, training will slow down and may even not converge as the agent is too busy trying out different routes all the time.</p>]]></content><author><name></name></author><category term="Reinforcement Learning"/><category term="Monte Carlo"/><category term="MC Control"/><category term="RL Basics"/><summary type="html"><![CDATA[Last time we looked at dynamic programming, methods that compute near-exact solutions to problems using a full-scale model of the environment dynamics. This time we are doing a complete 180. Monte Carlo(MC) methods are everything DP is not.]]></summary></entry><entry><title type="html">Dynamic Programming RL</title><link href="https://alshedivat.github.io/al-folio/blog/rl_dp/" rel="alternate" type="text/html" title="Dynamic Programming RL"/><published>2021-06-11T00:00:00+00:00</published><updated>2021-06-11T00:00:00+00:00</updated><id>https://alshedivat.github.io/al-folio/blog/dp</id><content type="html" xml:base="https://alshedivat.github.io/al-folio/blog/rl_dp/"><![CDATA[<p>Dynamic Programming(DP) in reinforcement learning refers to a set of algorithms that can be used to compute optimal policies when the agent knows everything about its surroundings; i.e. the agent has a perfect model of the environment. Although dynamic programming has a large number of drawbacks, it is the precursor to almost every other reinforcement learning algorithm. This makes knowing DP a necessity in understanding reinforcement learning. With that said, let’s begin.</p> <h2 id="dynamic-programming---a-brief-introduction">Dynamic Programming - A Brief Introduction</h2> <p><em>For those of you who know what dynamic programming is, feel free to skip this subsection.</em></p> <p>Dynamic programming, for the uninitiated, is reusing previously calculated values to compute the next value. Suppose we have to find the answer of <img src="https://render.githubusercontent.com/render/math?math=2 \times 2 \times 2"/> and <img src="https://render.githubusercontent.com/render/math?math=2 \times 2 \times 2 \times 2"/>. By calculating <img src="https://render.githubusercontent.com/render/math?math=2 \times 2 \times 2=8"/> first, we can be reuse it - <img src="https://render.githubusercontent.com/render/math?math=2 \times 2 \times 2 \times 2=8 \times 2=16"/> - to reduce the total number of multiplications. This makes problems that would otherwise grow exponentially with size to become polynomial - a major speed-up. In real terms, problems that may take over an hour to solve naively can be done in seconds.</p> <p>We perform dynamic programming by storing previously calculated values in a table. There are two main techniques of dynamic programming: tabulation and memoization. Both methods have their pros and cons. <a href="https://www.geeksforgeeks.org/">GeeksForGeeks</a> has a whole series on <a href="https://www.geeksforgeeks.org/dynamic-programming/">dynamic programming</a>, which goes in-depth into the topic.</p> <h1 id="the-theoretical-jargon">The Theoretical Jargon</h1> <p>To my utter dismay, we can’t proceed with this article without theory. Dynamic programming in RL has strict conditions we need to adhere to, which includes a lot of theory.</p> <h2 id="markov-decision-process-mdp">Markov Decision Process (MDP)</h2> <p>In the very first line of the article I’ve said that dynamic programming requires a <em>perfect model of the environment</em>. But what does this perfect model really mean?</p> <p>In our case, a perfect model means the agent has access to the likelihood of every action happening. The actions themselves must remain the same. Moreover, the probability of the agent moving from state <code class="language-plaintext highlighter-rouge">s</code> to state <code class="language-plaintext highlighter-rouge">s'</code> is conditionally independent - if the action takes place at time <code class="language-plaintext highlighter-rouge">t</code> it only depends on the probabilities at time <code class="language-plaintext highlighter-rouge">t-1</code>. The process is “memoryless”. The representation of the action space is a type of probability matrix a known as a <strong>Markov Chain</strong>.</p> <p>The importance of conditional independence may not be inherently obvious. An example of such independence would be in population growth. One only needs to know the current population and rate of natural increase(birth rate - death rate) to estimate the future population. Old data is not required. On the other hand, if you go to the doctor with a cough and chest pain, the doctor won’t just look at your current state and give you a prescription. He or she will have to look at your medical history. Prior data is a must.</p> <p>In terms of reinforcement learning, conditional independence simplifies much of the mathematics involved. If the agent had to take all his previous actions into consideration each time it took a new one, training would take forever. And while many processes may not satisfy this Markov property, working under that assumption usually leads to accurate results.</p> <p>A reinforcement learning problem that satisfies this Markov property is called a <strong>Markov Decision Process</strong>. A <em>finite</em> task is one that has a clear beginning and ending, i.e. there is a known number of possible states and actions. If the task is finite, the problem becomes a <em>finite MDP</em>. The agent interacts with the environment and receives feedback in the form of a reward. At each time step t, the environment changes due to the agent’s action. These changes are represented by the changing values in the Markov chain.</p> <figure> <img src="/images/rl/agent-environment-interaction.png" desc="The Agent Environment Interaction Diagram"/> <figcaption>The Agent Environment Interaction Diagram. Source: Sutton &amp; Barlo</figcaption> </figure> <h3 id="the-value-function">The Value Function</h3> <p>In reinforcement learning problem, our objective boils down to estimating the value of states (or of state-action pairs<sup><a href="#myfootnote1">1</a></sup>). The agent’s mapping of individual states to numbers is known as the <strong>value function</strong>. This value indicates “how good” a state is in terms of expected future reward.</p> <p>In practical terms a value function is a list of numbers: each number corresponding to the preference of a particular state. The process of refining the value function to reach the optimal policy is what dynamic programming is all about.</p> <h2 id="discounting---not-all-rewards-are-created-equal">Discounting - Not all rewards are created equal</h2> <p>By definition, the value of a state is its long-term reward potential. At time <code class="language-plaintext highlighter-rouge">t</code>, we need to consider the rewards of the states from time <code class="language-plaintext highlighter-rouge">t+1</code> to the end of the episode. But now a new question arises. <em>How should we differentiate between an instanteneous reward with one that is further into the future?</em> Depending on the task, we may choose to prioritize short-term rewards over long-term ones. The way we do this is by a process called <strong>discounting</strong>. The <em>expected<sup><a href="#myfootnote2">2</a></sup> discounted reward</em> at time <code class="language-plaintext highlighter-rouge">t</code>, <img src="https://render.githubusercontent.com/render/math?math=G_t"/> is</p> <p><img src="https://render.githubusercontent.com/render/math?math=G_t = R_{t %2B 1} %2B \gamma R_{t %2B 2} %2B \gamma^2 R_{t %2B 3} %2B \cdots = \sum_{k=0}^{\infty}\gamma^k R_{t %2B k %2B 1}" class="center"/></p> <p><img src="https://render.githubusercontent.com/render/math?math=\gamma"/> (<img src="https://render.githubusercontent.com/render/math?math=0 \leq \gamma \leq 1"/>) is known as the <strong>discount rate</strong>. <img src="https://render.githubusercontent.com/render/math?math=R_t"/> is the reward received at time <code class="language-plaintext highlighter-rouge">t</code>.</p> <p>The discount rate ensures that further the reward, the less is it is valued. This is particularly important if each episode is very long or infinite (the continuing task case). If we set <img src="https://render.githubusercontent.com/render/math?math=\gamma = 0"/>, the agent becomes <em>myopic</em>, only seeking to maximize the immediate reward at all times. In tasks such as pole balancing (the CartPole environment), a myopic strategy works perfectly well. And as <img src="https://render.githubusercontent.com/render/math?math=\gamma"/> approaches 1, the agent becomes more <em>far-sighted</em>. As long as the discount rate is less than 1, the infinite sum is bound to converge.</p> <h3 id="bellman-equations">Bellman Equations</h3> <p>Bellman optimality equations, or Bellman equations, are a set of equations defining the relationship between the value of a state and the value of its successor states. Solving this system of equations for a reinforcement learning task gives us the optimal value function.</p> <p>For the optimal policy <img src="https://render.githubusercontent.com/render/math?math=\pi_*"/> and an arbitrary state <code class="language-plaintext highlighter-rouge">s</code>, the optimal value function <img src="https://render.githubusercontent.com/render/math?math=V_{*}"/> under the Bellman optimality equation is</p> <figure> <img src="/images/rl/bellman_value_eq.png" desc="The Bellman Optimality Eqn for the state-value function"/> </figure> <p>Yes, the equation looks terrifying, but what it says is relatively straightforward: the value of a state under an optimal policy must equal the expected return for the best action from that state. At a state <code class="language-plaintext highlighter-rouge">s</code>, we choose the action <code class="language-plaintext highlighter-rouge">a</code> that gives the maximum reward <img src="https://render.githubusercontent.com/render/math?math=R_{t %2B 1}"/> plus the discounted reward from the next state <img src="https://render.githubusercontent.com/render/math?math=\gamma V_{*}(S_{t %2B 1})"/>.</p> <p>The Bellman optimality equation for the optimal state <img src="https://render.githubusercontent.com/render/math?math=s_{*}"/> is similar, only here we know the best action.</p> <figure> <img src="/images/rl/bellman_state_eq.png" desc="The Bellman Optimality Eqn for the state function"/> </figure> <p>Finding the inverse of the Markov chain will result in the solution of the Bellman equation for the value function. However, inverting a matrix is a slow, computationally expensive task. Dynamic programming algorithms attempt to find a good approximation to this inverse.</p> <p>Don’t worry too much if you don’t completely understand this subsection. The implementation is simpler than the equation.</p> <h1 id="walking-on-a-frozenlake">Walking on a FrozenLake</h1> <p>We will now go through and implement the dynamic programming algorithms on OpenAI Gym’s <a href="https://gym.openai.com/envs/FrozenLake-v0/">FrozenLake-v0</a> environment.</p> <h2 id="the-frozenlake">The FrozenLake</h2> <p>OpenAI’s FrozenLake environment is a 4 by 4 or 8 by 8 grid representing an icy lake. The goal here is to go from the starting position S to the finish F while avoiding the holes H. The lore is as follows:</p> <blockquote> <p>Winter is here. You and your friends were tossing around a frisbee at the park when you made a wild throw that left the frisbee out in the middle of the lake. The water is mostly frozen, but there are a few holes where the ice has melted. If you step into one of those holes, you’ll fall into the freezing water. At this time, there’s an international frisbee shortage, so it’s absolutely imperative that you navigate across the lake and retrieve the disc. However, the ice is slippery, so you won’t always move in the direction you intend. (Source: OpenAI)</p> </blockquote> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">env</span> <span class="o">=</span> <span class="n">gym</span><span class="p">.</span><span class="n">make</span><span class="p">(</span><span class="s">"FrozenLake-v0"</span><span class="p">)</span> 
<span class="n">env</span><span class="p">.</span><span class="n">reset</span><span class="p">()</span>
<span class="n">env</span><span class="p">.</span><span class="n">render</span><span class="p">()</span>
<span class="o">&gt;&gt;&gt;</span> <span class="n">SFFF</span>            <span class="c1"># (S: start)
</span>    <span class="n">FHFH</span>            <span class="c1"># (F: frozen, safe)
</span>    <span class="n">FFFH</span>            <span class="c1"># (H: hole, failure)
</span>    <span class="n">HFFG</span>            <span class="c1"># (G: goal)
</span></code></pre></div></div> <p>There are 16 possible positions the agent can be on, i.e. 16 possible states. The agent can take 4 actions: LEFT(0), DOWN(1), UP(2) and RIGHT(3).</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">num_states</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">observation_space</span><span class="p">.</span><span class="n">n</span>
<span class="n">num_actions</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">action_space</span><span class="p">.</span><span class="n">n</span>
<span class="k">print</span><span class="p">(</span><span class="n">num_states</span><span class="p">,</span> <span class="s">", "</span><span class="p">,</span> <span class="n">num_actions</span><span class="p">)</span>
<span class="o">&gt;&gt;&gt;</span> <span class="mi">16</span><span class="p">,</span> <span class="mi">4</span>
</code></pre></div></div> <p>The special factor about this environment is that the agent often does not do what we tell it to. I demonstrate this below</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">state</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">reset</span><span class="p">()</span>
<span class="k">print</span><span class="p">(</span><span class="n">state</span><span class="p">)</span>
<span class="o">&gt;&gt;&gt;</span> <span class="mi">0</span>        <span class="c1"># Starting position
# Let's make the agent go a step downwards
</span><span class="n">new_state</span><span class="p">,</span> <span class="n">reward</span><span class="p">,</span> <span class="n">is_done</span><span class="p">,</span> <span class="n">info</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">step</span><span class="p">(</span><span class="mi">1</span><span class="p">)</span>  
<span class="k">print</span><span class="p">(</span><span class="n">new_state</span><span class="p">)</span>  
<span class="o">&gt;&gt;&gt;</span> <span class="mi">1</span>       <span class="c1"># The agent went to the left instead
</span><span class="k">print</span><span class="p">(</span><span class="n">reward</span><span class="p">)</span>
<span class="o">&gt;&gt;&gt;</span> <span class="mi">0</span>     
</code></pre></div></div> <p>The agent went the other way instead. Let’s print the <code class="language-plaintext highlighter-rouge">info</code> variable to see the reason.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">print</span><span class="p">(</span><span class="n">info</span><span class="p">)</span>
<span class="o">&gt;&gt;&gt;</span> <span class="p">{</span><span class="s">'prob'</span><span class="p">:</span> <span class="mf">0.3333333333333333</span><span class="p">}</span>
</code></pre></div></div> <p>The probability of the agent performing a particular action is 1/3. Not the easiest environment to traverse. The reward is 1 at the goal, and 0 everywhere else. This makes the environment impossible for the random search algorithm to solve. Feel free to try it out on your own. I personally found my agent successfully traversing FrozenLake less than 15% of the time (given 1000 attempts).</p> <h2 id="iteration-evaluation-and-improvement">Iteration: Evaluation and Improvement</h2> <p>Iteration, evaluation, and improvement were things I often mixed up when I first started learning this topic. We evaluate policies. By this I mean that for a new policy, we update our value function based on the new action taken and the previous set of values. This update process follows the <em>Bellman Optimality Equation for the value function</em>.</p> <p>After we have our updated values, we use this to <em>improve</em> our policy. This is done by choosing the actions that have the highest updated values (greedy). The equation used here is the <em>Bellman Optimality Equation for the optimal state</em>.</p> <p>Now that we have a new policy, we evaluate this policy again. This cyclic process is known as <strong>iteration</strong>. Starting with arbitrary value and policy functions, the iteration process leads us to the optimal <img src="https://render.githubusercontent.com/render/math?math=V_{*}"/> and <img src="https://render.githubusercontent.com/render/math?math=\pi_{*}"/> sooner or later.</p> <table> <thead> <tr> <th style="text-align: center">Policy improvement cycle</th> <th style="text-align: center">The goal of policy improvement</th> </tr> </thead> <tbody> <tr> <td style="text-align: center"><img src="/images/rl/iteration.png" alt="Policy Improvement Cycle"/></td> <td style="text-align: center"><img src="/images/rl/iteration_goal.png" alt="Policy Improvement Goal"/></td> </tr> </tbody> </table> <p>&nbsp;</p> <h2 id="policy-evaluation">Policy Evaluation</h2> <p>Firstly let’s go through the policy evaluation algorithm. The task here is to compute the value function for the policy π using the Bellman equation. The value function is computed iteratively, that is, we repeatedly keep running the equation until the function converges (the difference is less than a threshold value).</p> <p>Before we start, there is one aspect to this algorithm (and the subsequent ones) that make it a dynamic programming algorithm. That is complete environment knowledge. In toy problems this knowledge of action probabilities is often called the “transformation matrix”. As for the FrozenLake environment, this is how we access it.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="nn">pprint</span> <span class="kn">import</span> <span class="n">pprint</span>  <span class="c1"># Cleaner print formatting
# Let's print the action probabilities at position 0
</span><span class="n">pprint</span><span class="p">(</span><span class="n">env</span><span class="p">.</span><span class="n">env</span><span class="p">.</span><span class="n">P</span><span class="p">[</span><span class="mi">0</span><span class="p">])</span>
<span class="o">&gt;&gt;&gt;</span> <span class="p">{</span><span class="mi">0</span><span class="p">:</span> <span class="p">[(</span><span class="mf">0.3333333333333333</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mf">0.0</span><span class="p">,</span> <span class="bp">False</span><span class="p">),</span>
     <span class="p">(</span><span class="mf">0.3333333333333333</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mf">0.0</span><span class="p">,</span> <span class="bp">False</span><span class="p">),</span>
        <span class="p">(</span><span class="mf">0.3333333333333333</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="mf">0.0</span><span class="p">,</span> <span class="bp">False</span><span class="p">)],</span>
    <span class="mi">1</span><span class="p">:</span> <span class="p">[(</span><span class="mf">0.3333333333333333</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mf">0.0</span><span class="p">,</span> <span class="bp">False</span><span class="p">),</span>
        <span class="p">(</span><span class="mf">0.3333333333333333</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="mf">0.0</span><span class="p">,</span> <span class="bp">False</span><span class="p">),</span>
        <span class="p">(</span><span class="mf">0.3333333333333333</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="mf">0.0</span><span class="p">,</span> <span class="bp">False</span><span class="p">)],</span>
    <span class="mi">2</span><span class="p">:</span> <span class="p">[(</span><span class="mf">0.3333333333333333</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="mf">0.0</span><span class="p">,</span> <span class="bp">False</span><span class="p">),</span>
        <span class="p">(</span><span class="mf">0.3333333333333333</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="mf">0.0</span><span class="p">,</span> <span class="bp">False</span><span class="p">),</span>
        <span class="p">(</span><span class="mf">0.3333333333333333</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mf">0.0</span><span class="p">,</span> <span class="bp">False</span><span class="p">)],</span>
    <span class="mi">3</span><span class="p">:</span> <span class="p">[(</span><span class="mf">0.3333333333333333</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="mf">0.0</span><span class="p">,</span> <span class="bp">False</span><span class="p">),</span>
        <span class="p">(</span><span class="mf">0.3333333333333333</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mf">0.0</span><span class="p">,</span> <span class="bp">False</span><span class="p">),</span>
        <span class="p">(</span><span class="mf">0.3333333333333333</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mf">0.0</span><span class="p">,</span> <span class="bp">False</span><span class="p">)]}</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">env.env.P</code> is a dictionary where the keys are states and values are dictionaries of possible actions and their outcomes. Each action has a probability of 1/3 of happening, the next value represents the resulting position the agent might land on (it can go to either to the right - 1 - or down - 4). The 3rd value is the reward for that action which is 0 everywhere except the goal. The final value is a boolean that indicates whether the episode ends as a result of that action. This happens when the agent reaches the goal or falls into a hole. Now let’s look that the probabilities at position 5, a hole.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Let's print the action probabilities at position 5
</span><span class="n">pprint</span><span class="p">(</span><span class="n">env</span><span class="p">.</span><span class="n">env</span><span class="p">.</span><span class="n">P</span><span class="p">[</span><span class="mi">5</span><span class="p">])</span>
<span class="o">&gt;&gt;&gt;</span> <span class="p">{</span><span class="mi">0</span><span class="p">:</span> <span class="p">[(</span><span class="mf">1.0</span><span class="p">,</span> <span class="mi">5</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="bp">True</span><span class="p">)],</span>
    <span class="mi">1</span><span class="p">:</span> <span class="p">[(</span><span class="mf">1.0</span><span class="p">,</span> <span class="mi">5</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="bp">True</span><span class="p">)],</span>
    <span class="mi">2</span><span class="p">:</span> <span class="p">[(</span><span class="mf">1.0</span><span class="p">,</span> <span class="mi">5</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="bp">True</span><span class="p">)],</span>
    <span class="mi">3</span><span class="p">:</span> <span class="p">[(</span><span class="mf">1.0</span><span class="p">,</span> <span class="mi">5</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="bp">True</span><span class="p">)]}</span>
</code></pre></div></div> <p>Once the agent has fallen into a hole, it can’t get out. The episode terminates. There is no reward. When approaching the goal, for example in position 14, some outcomes have a reward of 1. Feel free to look at other positions to see what their outcomes are.</p> <p>Getting back to the topic, I will first write down the function and then we can go through it in parts.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">threshold</span> <span class="o">=</span> <span class="mf">0.0001</span>      <span class="c1"># convergence threshold
</span><span class="n">gamma</span> <span class="o">=</span> <span class="mf">0.99</span>            <span class="c1"># discount rate
</span></code></pre></div></div> <p>The two parameters we set are the discount rate and convergence threshold. The threshold defines a stopping point for the function. The smaller the threshold, the more accurate the values, but the longer the time taken to converge. A gamma of 0.99 is suitable for this environment, but feel free to try out different values and see what works best.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">policy_evaluation</span><span class="p">(</span><span class="n">env</span><span class="p">,</span> <span class="n">policy</span><span class="p">,</span> <span class="n">gamma</span><span class="p">,</span> <span class="n">threshold</span><span class="p">):</span>
    <span class="n">num_states</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">observation_space</span><span class="p">.</span><span class="n">n</span>
    <span class="n">V</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">zeros</span><span class="p">(</span><span class="n">num_states</span><span class="p">)</span>
    <span class="n">max_delta</span> <span class="o">=</span> <span class="n">threshold</span> <span class="o">+</span> <span class="mi">1</span>
    <span class="k">while</span> <span class="n">max_delta</span> <span class="o">&gt;</span> <span class="n">threshold</span><span class="p">:</span>
        <span class="n">temp</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">zeros</span><span class="p">(</span><span class="n">num_states</span><span class="p">)</span>
        <span class="k">for</span> <span class="n">state</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="n">num_states</span><span class="p">):</span>
            <span class="n">action</span> <span class="o">=</span> <span class="n">policy</span><span class="p">[</span><span class="n">state</span><span class="p">].</span><span class="n">item</span><span class="p">()</span>
            <span class="k">for</span> <span class="n">proba</span><span class="p">,</span> <span class="n">new_state</span><span class="p">,</span> <span class="n">reward</span><span class="p">,</span> <span class="n">_</span> <span class="ow">in</span> <span class="n">env</span><span class="p">.</span><span class="n">env</span><span class="p">.</span><span class="n">P</span><span class="p">[</span><span class="n">state</span><span class="p">][</span><span class="n">action</span><span class="p">]:</span>
                <span class="n">temp</span><span class="p">[</span><span class="n">state</span><span class="p">]</span> <span class="o">+=</span> <span class="n">proba</span> <span class="o">*</span> <span class="p">(</span><span class="n">reward</span> <span class="o">+</span> <span class="n">V</span><span class="p">[</span><span class="n">new_state</span><span class="p">]</span> <span class="o">*</span> <span class="n">gamma</span><span class="p">)</span>
        <span class="n">max_delta</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nb">max</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nb">abs</span><span class="p">(</span><span class="n">V</span> <span class="o">-</span> <span class="n">temp</span><span class="p">))</span>
        <span class="n">V</span> <span class="o">=</span> <span class="n">temp</span><span class="p">.</span><span class="n">clone</span><span class="p">()</span>
    <span class="k">return</span> <span class="n">V</span>
</code></pre></div></div> <p>We first define our value array <code class="language-plaintext highlighter-rouge">V</code> as a 1D matrix of 0s. <code class="language-plaintext highlighter-rouge">max_delta</code> stores the maximum difference in value for a state between two consecutive iterations. We use this to check for convergence. <code class="language-plaintext highlighter-rouge">temp</code> is the new value function that is temporary defined as another 1D matrix of 0s.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="n">state</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="n">num_states</span><span class="p">):</span>
    <span class="n">action</span> <span class="o">=</span> <span class="n">policy</span><span class="p">[</span><span class="n">state</span><span class="p">].</span><span class="n">item</span><span class="p">()</span> <span class="c1"># item() converts Tensor to int
</span></code></pre></div></div> <p>For each state, we choose the action specified by the policy function.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="n">proba</span><span class="p">,</span> <span class="n">new_state</span><span class="p">,</span> <span class="n">reward</span><span class="p">,</span> <span class="n">_</span> <span class="ow">in</span> <span class="n">env</span><span class="p">.</span><span class="n">env</span><span class="p">.</span><span class="n">P</span><span class="p">[</span><span class="n">state</span><span class="p">][</span><span class="n">action</span><span class="p">]:</span>
</code></pre></div></div> <p>As we have seen before, P is a dictionary of dictionaries. We are taking the possible actions in <code class="language-plaintext highlighter-rouge">P[state]</code> and then iterating through the possible outcomes for an action in <code class="language-plaintext highlighter-rouge">P[state][action]</code>.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">temp</span><span class="p">[</span><span class="n">state</span><span class="p">]</span> <span class="o">+=</span> <span class="n">proba</span> <span class="o">*</span> <span class="p">(</span><span class="n">reward</span> <span class="o">+</span> <span class="n">V</span><span class="p">[</span><span class="n">new_state</span><span class="p">]</span> <span class="o">*</span> <span class="n">gamma</span><span class="p">)</span>
</code></pre></div></div> <p>The value function for a state is the expected sum (<code class="language-plaintext highlighter-rouge">+= proba *</code>) of the reward (<code class="language-plaintext highlighter-rouge">reward + </code>) and discounted future rewards (<code class="language-plaintext highlighter-rouge">V[new_state] * gamma</code>).</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">max_delta</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nb">max</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nb">abs</span><span class="p">(</span><span class="n">V</span> <span class="o">-</span> <span class="n">temp</span><span class="p">))</span>
</code></pre></div></div> <p>This line computes the maximum absolute difference between the value function at the current iteration <code class="language-plaintext highlighter-rouge">temp</code> against the value function at the previous iteration <code class="language-plaintext highlighter-rouge">v</code>. <code class="language-plaintext highlighter-rouge">V = temp.clone()</code> simply copies the new value function onto the old one.</p> <p>If you have understood everything up to this point, rejoice! We are almost at the finish line.</p> <h2 id="policy-iteration">Policy Iteration</h2> <p>Policy iteration consists of policy evaluation and policy improvement. Now that we are done with policy evaluation, the next step in this cycle is policy improvement.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">policy_improvement</span><span class="p">(</span><span class="n">env</span><span class="p">,</span> <span class="n">V</span><span class="p">,</span> <span class="n">gamma</span><span class="p">):</span>
    <span class="n">num_states</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">observation_space</span><span class="p">.</span><span class="n">n</span>
    <span class="n">num_actions</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">action_space</span><span class="p">.</span><span class="n">n</span>
    <span class="n">policy</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">zeros</span><span class="p">(</span><span class="n">num_states</span><span class="p">)</span>
    <span class="k">for</span> <span class="n">state</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="n">num_states</span><span class="p">):</span>
        <span class="n">actions</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">zeros</span><span class="p">(</span><span class="n">num_actions</span><span class="p">)</span>
        <span class="k">for</span> <span class="n">action</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="n">num_actions</span><span class="p">):</span>
            <span class="k">for</span> <span class="n">proba</span><span class="p">,</span> <span class="n">new_state</span><span class="p">,</span> <span class="n">reward</span><span class="p">,</span> <span class="n">_</span> <span class="ow">in</span> <span class="n">env</span><span class="p">.</span><span class="n">env</span><span class="p">.</span><span class="n">P</span><span class="p">[</span><span class="n">state</span><span class="p">][</span><span class="n">action</span><span class="p">]:</span>
                <span class="n">actions</span><span class="p">[</span><span class="n">action</span><span class="p">]</span> <span class="o">+=</span> <span class="n">proba</span> <span class="o">*</span> <span class="p">(</span><span class="n">reward</span> <span class="o">+</span> <span class="n">V</span><span class="p">[</span><span class="n">new_state</span><span class="p">]</span> <span class="o">*</span> <span class="n">gamma</span><span class="p">)</span>
        <span class="n">policy</span><span class="p">[</span><span class="n">state</span><span class="p">]</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">argmax</span><span class="p">(</span><span class="n">actions</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">policy</span>
</code></pre></div></div> <p>Here, we set our policy function to a matrix of 0s.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="n">state</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="n">num_states</span><span class="p">):</span>
    <span class="n">actions</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">zeros</span><span class="p">(</span><span class="n">num_actions</span><span class="p">)</span>
</code></pre></div></div> <p>A matrix of 0s corresponding to the available actions is initialized.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="n">action</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="n">num_actions</span><span class="p">):</span>
    <span class="k">for</span> <span class="n">proba</span><span class="p">,</span> <span class="n">new_state</span><span class="p">,</span> <span class="n">reward</span><span class="p">,</span> <span class="n">_</span> <span class="ow">in</span> <span class="n">env</span><span class="p">.</span><span class="n">env</span><span class="p">.</span><span class="n">P</span><span class="p">[</span><span class="n">state</span><span class="p">][</span><span class="n">action</span><span class="p">]:</span>
        <span class="n">actions</span><span class="p">[</span><span class="n">action</span><span class="p">]</span> <span class="o">+=</span> <span class="n">proba</span> <span class="o">*</span> <span class="p">(</span><span class="n">reward</span> <span class="o">+</span> <span class="n">V</span><span class="p">[</span><span class="n">new_state</span><span class="p">]</span> <span class="o">*</span> <span class="n">gamma</span><span class="p">)</span>
<span class="n">policy</span><span class="p">[</span><span class="n">state</span><span class="p">]</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">argmax</span><span class="p">(</span><span class="n">actions</span><span class="p">)</span>
</code></pre></div></div> <p>This is part of the Bellman optimality equation for the policy function. The value of each action at the given state is updated by the expected current reward and the discounted future reward. After the value of each action at the given state is calculated, the action with the highest reward is selected using <code class="language-plaintext highlighter-rouge">torch.argmax(actions)</code>.</p> <p>The policy iteration algorithm alternates between these two functions until the optimal policy is reached.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">policy_iteration</span><span class="p">(</span><span class="n">env</span><span class="p">,</span> <span class="n">gamma</span><span class="o">=</span><span class="mf">0.99</span><span class="p">,</span> <span class="n">threshold</span><span class="o">=</span><span class="mf">0.0001</span><span class="p">):</span>
    <span class="n">num_states</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">observation_space</span><span class="p">.</span><span class="n">n</span>
    <span class="n">num_actions</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">action_space</span><span class="p">.</span><span class="n">n</span>
    <span class="n">policy</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">randint</span><span class="p">(</span><span class="n">high</span><span class="o">=</span><span class="n">num_actions</span><span class="p">,</span> <span class="n">size</span><span class="o">=</span><span class="p">(</span><span class="n">num_states</span><span class="p">,)).</span><span class="nb">float</span><span class="p">()</span>
    <span class="k">while</span> <span class="bp">True</span><span class="p">:</span>
        <span class="n">V</span> <span class="o">=</span> <span class="n">policy_evaluation</span><span class="p">(</span><span class="n">env</span><span class="p">,</span> <span class="n">policy</span><span class="p">,</span> <span class="n">gamma</span><span class="o">=</span><span class="n">gamma</span><span class="p">,</span> <span class="n">threshold</span><span class="o">=</span><span class="n">threshold</span><span class="p">)</span>
        <span class="n">new_policy</span> <span class="o">=</span> <span class="n">policy_improvement</span><span class="p">(</span><span class="n">env</span><span class="p">,</span> <span class="n">V</span><span class="p">,</span> <span class="n">gamma</span><span class="o">=</span><span class="n">gamma</span><span class="p">)</span>
        <span class="k">if</span> <span class="n">torch</span><span class="p">.</span><span class="n">equal</span><span class="p">(</span><span class="n">new_policy</span><span class="p">,</span> <span class="n">policy</span><span class="p">):</span>
            <span class="k">return</span> <span class="n">V</span><span class="p">,</span> <span class="n">new_policy</span>
        <span class="n">policy</span> <span class="o">=</span> <span class="n">new_policy</span><span class="p">.</span><span class="n">clone</span><span class="p">()</span>
</code></pre></div></div> <p>Using <code class="language-plaintext highlighter-rouge">torch.randint()</code> we create an arbitrary policy of random numbers (in the range 0, num_actions). We keep running the policy evaluation -&gt; policy improvement loop until there is no improvement in our policy <code class="language-plaintext highlighter-rouge">torch.equal(new_policy, policy)</code>.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">V_optimal</span><span class="p">,</span> <span class="n">optimal_policy</span> <span class="o">=</span> <span class="n">policy_iteration</span><span class="p">(</span><span class="n">env</span><span class="p">)</span>
<span class="n">total_reward</span> <span class="o">=</span> <span class="p">[]</span>
<span class="k">for</span> <span class="n">n</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="n">NUM_EPISODES</span><span class="p">):</span>
    <span class="n">total_reward</span><span class="p">.</span><span class="n">append</span><span class="p">(</span><span class="n">run_episode</span><span class="p">(</span><span class="n">env</span><span class="p">,</span> <span class="n">optimal_policy</span><span class="p">))</span>
<span class="k">print</span><span class="p">(</span><span class="sa">f</span><span class="s">"Success rate over </span><span class="si">{</span><span class="n">NUM_EPISODES</span><span class="si">}</span><span class="s"> episodes: </span><span class="si">{</span><span class="nb">sum</span><span class="p">(</span><span class="n">total_reward</span><span class="p">)</span> <span class="o">*</span> <span class="mi">100</span> <span class="o">/</span> <span class="n">NUM_EPISODES</span><span class="si">}</span><span class="s">%"</span><span class="p">)</span>
<span class="o">&gt;&gt;&gt;</span> <span class="n">Success</span> <span class="n">rate</span> <span class="n">over</span> <span class="mi">1000</span> <span class="n">episodes</span><span class="p">:</span> <span class="mf">84.3</span><span class="o">%</span>
</code></pre></div></div> <p>As we can see, policy iteration has a tremendous success rate (84.3% vs &lt;15%) over the random strategy.</p> <p>Policy iteration is particularly useful when we already know a starting policy, i.e. we know a decent strategy to solve the problem. In addition, if the environment has a large number of possible actions, this method converges faster than value iteration.</p> <h2 id="value-iteration">Value Iteration</h2> <p>Value iteration is similar to policy iteration. But where policy iteration uses the Bellman optimality equation for the policy function, value iteration uses it for the value function. This method is marginally better when there are fewer actions to deal with.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">value_iteration</span><span class="p">(</span><span class="n">env</span><span class="p">,</span> <span class="n">gamma</span><span class="o">=</span><span class="mf">0.99</span><span class="p">,</span> <span class="n">thres</span> <span class="o">=</span> <span class="mf">0.0001</span><span class="p">):</span>
    <span class="n">num_states</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">observation_space</span><span class="p">.</span><span class="n">n</span>
    <span class="n">num_actions</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">action_space</span><span class="p">.</span><span class="n">n</span>
    <span class="n">V</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">zeros</span><span class="p">(</span><span class="n">num_states</span><span class="p">)</span>
    <span class="n">max_delta</span> <span class="o">=</span> <span class="n">thres</span> <span class="o">+</span> <span class="mi">1</span>
    <span class="k">while</span> <span class="n">max_delta</span> <span class="o">&gt;</span> <span class="n">thres</span><span class="p">:</span>
        <span class="n">temp</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">zeros</span><span class="p">(</span><span class="n">num_states</span><span class="p">)</span>
        <span class="k">for</span> <span class="n">state</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="n">num_states</span><span class="p">):</span>
            <span class="n">v_actions</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">zeros</span><span class="p">(</span><span class="n">num_actions</span><span class="p">)</span>
            <span class="k">for</span> <span class="n">action</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="n">num_actions</span><span class="p">):</span>
                <span class="k">for</span> <span class="n">proba</span><span class="p">,</span> <span class="n">new_state</span><span class="p">,</span> <span class="n">reward</span><span class="p">,</span> <span class="n">is_done</span> <span class="ow">in</span> <span class="n">env</span><span class="p">.</span><span class="n">env</span><span class="p">.</span><span class="n">P</span><span class="p">[</span><span class="n">state</span><span class="p">][</span><span class="n">action</span><span class="p">]:</span>
                    <span class="n">v_actions</span><span class="p">[</span><span class="n">action</span><span class="p">]</span> <span class="o">+=</span> <span class="n">proba</span> <span class="o">*</span> <span class="p">(</span><span class="n">reward</span> <span class="o">+</span> <span class="n">gamma</span> <span class="o">*</span> <span class="n">V</span><span class="p">[</span><span class="n">new_state</span><span class="p">])</span>    <span class="c1"># Value iteration 
</span>            <span class="n">temp</span><span class="p">[</span><span class="n">state</span><span class="p">]</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nb">max</span><span class="p">(</span><span class="n">v_actions</span><span class="p">)</span>              <span class="c1"># Select the action with the highest reward
</span>        <span class="n">max_delta</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nb">max</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nb">abs</span><span class="p">(</span><span class="n">V</span> <span class="o">-</span> <span class="n">temp</span><span class="p">))</span>
        <span class="n">V</span> <span class="o">=</span> <span class="n">temp</span><span class="p">.</span><span class="n">clone</span><span class="p">()</span>
    <span class="k">return</span> <span class="n">V</span>
</code></pre></div></div> <p>As you can see, this algorithm is very similar to that of policy evaluation. There are some major differences.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">temp</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">zeros</span><span class="p">(</span><span class="n">num_states</span><span class="p">)</span>
<span class="k">for</span> <span class="n">state</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="n">num_states</span><span class="p">):</span>
    <span class="n">v_actions</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">zeros</span><span class="p">(</span><span class="n">num_actions</span><span class="p">)</span>
    <span class="k">for</span> <span class="n">action</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="n">num_actions</span><span class="p">):</span>
        <span class="k">for</span> <span class="n">proba</span><span class="p">,</span> <span class="n">new_state</span><span class="p">,</span> <span class="n">reward</span><span class="p">,</span> <span class="n">is_done</span> <span class="ow">in</span> <span class="n">env</span><span class="p">.</span><span class="n">env</span><span class="p">.</span><span class="n">P</span><span class="p">[</span><span class="n">state</span><span class="p">][</span><span class="n">action</span><span class="p">]:</span>
            <span class="n">v_actions</span><span class="p">[</span><span class="n">action</span><span class="p">]</span> <span class="o">+=</span> <span class="n">proba</span> <span class="o">*</span> <span class="p">(</span><span class="n">reward</span> <span class="o">+</span> <span class="n">gamma</span> <span class="o">*</span> <span class="n">V</span><span class="p">[</span><span class="n">new_state</span><span class="p">])</span>
    <span class="n">temp</span><span class="p">[</span><span class="n">state</span><span class="p">]</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nb">max</span><span class="p">(</span><span class="n">v_actions</span><span class="p">)</span>
</code></pre></div></div> <p>Instead of selecting an action from a given policy, we calculate the values of all policies using the value matrix of the previous iteration and then choose the greatest calculated value as the value of that state. This is the Bellman optimality equation for the value function, stripped of all of its mathematical illegibility. <code class="language-plaintext highlighter-rouge">v_actions[action] += proba * (reward + gamma * V[new_state])</code> calculates the expected value for each state, while <code class="language-plaintext highlighter-rouge">temp[state] = torch.max(v_actions)</code> greedily selects the max value from the matrix of action values.</p> <p>Extracting the optimal policy here is exactly the same as the policy improvement function. I am including it here separately for completeness.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">extract_optimal_policy</span><span class="p">(</span><span class="n">env</span><span class="p">,</span> <span class="n">V</span><span class="p">,</span> <span class="n">gamma</span><span class="o">=</span><span class="mf">0.99</span><span class="p">):</span>
    <span class="n">num_states</span><span class="p">,</span> <span class="n">num_actions</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">observation_space</span><span class="p">.</span><span class="n">n</span><span class="p">,</span> <span class="n">env</span><span class="p">.</span><span class="n">action_space</span><span class="p">.</span><span class="n">n</span>
    <span class="n">optimal_policy</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">zeros</span><span class="p">(</span><span class="n">num_states</span><span class="p">)</span>
    <span class="k">for</span> <span class="n">state</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="n">num_states</span><span class="p">):</span>
        <span class="n">v_actions</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">zeros</span><span class="p">(</span><span class="n">num_actions</span><span class="p">)</span>
        <span class="k">for</span> <span class="n">action</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="n">num_actions</span><span class="p">):</span>
            <span class="k">for</span> <span class="n">proba</span><span class="p">,</span> <span class="n">new_state</span><span class="p">,</span> <span class="n">reward</span><span class="p">,</span> <span class="n">_</span> <span class="ow">in</span> <span class="n">env</span><span class="p">.</span><span class="n">env</span><span class="p">.</span><span class="n">P</span><span class="p">[</span><span class="n">state</span><span class="p">][</span><span class="n">action</span><span class="p">]:</span>
                <span class="n">v_actions</span><span class="p">[</span><span class="n">action</span><span class="p">]</span> <span class="o">+=</span> <span class="n">proba</span> <span class="o">*</span> <span class="p">(</span><span class="n">reward</span> <span class="o">+</span> <span class="n">gamma</span> <span class="o">*</span> <span class="n">V</span><span class="p">[</span><span class="n">new_state</span><span class="p">])</span>
        <span class="n">optimal_policy</span><span class="p">[</span><span class="n">state</span><span class="p">]</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">argmax</span><span class="p">(</span><span class="n">v_actions</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">optimal_policy</span>
</code></pre></div></div> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">optimal_values</span> <span class="o">=</span> <span class="n">value_iteration</span><span class="p">(</span><span class="n">env</span><span class="p">)</span>
<span class="n">optimal_policy</span> <span class="o">=</span> <span class="n">extract_optimal_policy</span><span class="p">(</span><span class="n">env</span><span class="p">,</span> <span class="n">optimal_values</span><span class="p">)</span>
<span class="n">total_reward</span> <span class="o">=</span> <span class="p">[]</span>
<span class="k">for</span> <span class="n">n</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="n">NUM_EPISODES</span><span class="p">):</span>
    <span class="n">total_reward</span><span class="p">.</span><span class="n">append</span><span class="p">(</span><span class="n">run_episode</span><span class="p">(</span><span class="n">env</span><span class="p">,</span> <span class="n">optimal_policy</span><span class="p">))</span>
<span class="k">print</span><span class="p">(</span><span class="sa">f</span><span class="s">"Success rate over </span><span class="si">{</span><span class="n">NUM_EPISODES</span><span class="si">}</span><span class="s"> episodes: </span><span class="si">{</span><span class="nb">sum</span><span class="p">(</span><span class="n">total_reward</span><span class="p">)</span> <span class="o">*</span> <span class="mi">100</span> <span class="o">/</span> <span class="n">NUM_EPISODES</span><span class="si">}</span><span class="s">%"</span><span class="p">)</span>
<span class="o">&gt;&gt;&gt;</span> <span class="n">Success</span> <span class="n">rate</span> <span class="n">over</span> <span class="mi">1000</span> <span class="n">episodes</span><span class="p">:</span> <span class="mf">87.3</span><span class="o">%</span>
</code></pre></div></div> <p>Value iteration does 3% better than policy iteration here, and is slightly faster to train.</p> <h1 id="conclusion">Conclusion</h1> <p>Dynamic programming uses complete environment knowledge to accurately compute optimal values and policies with straightforward methods. The methods used go through all possible actions and futures in each state, doing what is known as <em>full backups</em>. It goes without saying that this is not the most efficient way of doing things. Moreover, this makes DP methods not scale to larger problems; DP specially struggles in problems with a large number of states.</p> <p>Policy evaluation iteratively computes the value function for a given policy, while policy improvement calculates an improved policy from a given value function. These methods combine to form policy and value iteration, the main DP algorithms.</p> <p>DP uses estimates of future values to estimate the present value. This method of <em>bootstrapping</em> is used in many other reinforcement learning algorithms, namely in temporal difference learning methods.</p> <p>The next article will focus on Monte Carlo methods, which not just not require full environment knowledge, don’t even need a model of the environment to work. Monte Carlo methods are very efficient and easily scale to large problems.</p> <h3 id="footnotes">Footnotes</h3> <p><a name="myfootnote1">[1]</a> A state-action pair is the grouping of a state and a particular action into a single value. In a backup diagram, this would be the difference between assigning values to states themselves or to actions (state transitions).</p> <p><a name="myfootnote2">[2]</a> The term <em>expected return</em> is used because we are calculating future rewards, which by nature is uncertain. Therefore, the expected return is a more sensible measure compared to trying to calculating the maximum reward from each state.</p>]]></content><author><name></name></author><category term="Reinforcement Learning"/><category term="Dynamic Programming"/><category term="Markov Decision Process"/><category term="RL Basics"/><summary type="html"><![CDATA[Dynamic Programming(DP) in reinforcement learning refers to a set of algorithms that can be used to compute optimal policies when the agent knows everything about its surroundings; i.e. the agent has a perfect model of the environment. Although dynamic programming has a large number of drawbacks, it is the precursor to almost every other reinforcement learning algorithm. This makes knowing DP a necessity in understanding reinforcement learning. With that said, let’s begin.]]></summary></entry><entry><title type="html">The OpenAI Gym</title><link href="https://alshedivat.github.io/al-folio/blog/rl_gym/" rel="alternate" type="text/html" title="The OpenAI Gym"/><published>2021-06-05T00:00:00+00:00</published><updated>2021-06-05T00:00:00+00:00</updated><id>https://alshedivat.github.io/al-folio/blog/rl_gym</id><content type="html" xml:base="https://alshedivat.github.io/al-folio/blog/rl_gym/"><![CDATA[<figure> <img src="/images/rl/gym.png" desc="The OpenAI Gym"/> <figcaption>Is this working out?</figcaption> </figure> <p>The OpenAI Gym is a fascinating place. The Gym makes playing with reinforcement learning models fun and interactive without having to deal with the hassle of setting up environments. While you could argue that creating your own environments is a pretty important skill, do you really want to spend a week in something like PyGame just to start a project? No? Well let’s get right to it then.</p> <p>According to OpenAI, <em>Gym is a toolkit for developing and comparing reinforcement learning algorithms. It supports teaching agents everything from walking to playing games like Pong or Pinball.</em> And it is, to date, the best place to play around with RL. It is easy to use, well-documented, and very customizable. Go to the <a href="https://github.com/openai/gym/wiki/Table-of-environments">Table of Environments</a> for a breakdown of all they have to offer. Without further ado, let’s get to work!</p> <h2 id="setup">Setup</h2> <p>To start working with the OpenAI Gym, we need two libraries:</p> <ul> <li><a href="www.anaconda.com/"><strong>Anaconda</strong></a>: This is an open-source Python distribution for data science and machine learning. Anaconda has its own package manager <code class="language-plaintext highlighter-rouge">conda</code>, which works alongside <code class="language-plaintext highlighter-rouge">pip</code>. The installation process is simple and the <a href="https://www.anaconda.com/products/individual">Products page</a> has easy-to-use installers.</li> <li><a href="https://pytorch.org/"><strong>PyTorch</strong></a><sup><a href="#myfootnote1">1</a></sup>: This is an AI/ML library from the Facebook AI Research(FAIR) group. It is similar to NumPy in matrix manipulation, but is more optimized and allows GPU support. Just go to <a href="https://pytorch.org/">the Pytorch website</a> and select your system. CUDA is for NVidia GPUs, so if you don’t have one or have an AMD GPU (like me), select the CPU option.</li> </ul> <figure> <img src="/images/rl/pytorch_install.png" desc="Installing PyTorch"/> <figcaption>Select the relevant options from the website and run the resulting line of code on your system.</figcaption> </figure> <h2 id="going-to-the-gym">Going to the Gym</h2> <p>Setting up the Gym and running environments is a very simple process.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip install gym
</code></pre></div></div> <p>That’s it.</p> <p>That was Part 1 of the setup. Next we need to install a few modules. Here I will be showing you the Box2D (Lunar Lander) and Atari (All Atari games) environment setups for now.</p> <p>For Box2D:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda install swig
pip install box2d
pip install gym[box2d]
</code></pre></div></div> <p>For Atari, there are two ways:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip install gym[atari]
</code></pre></div></div> <p>If this throws any errors, use this instead:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip install -f https://github.com/Kojoley/atari-py/releases atari_py 
</code></pre></div></div> <p>On Windows systems, you may need to install Microsoft Visual Studio Build Tools (<a href="https://visualstudio.microsoft.com/visual-cpp-build-tools/">from here</a>).This will be the case if you see any errors involving <code class="language-plaintext highlighter-rouge">\atari_py\ale_interface\ale_c.dll</code>.</p> <h2 id="first-steps">First Steps</h2> <p>Let’s start with something iconic from the Atari 2600, <strong>Ms. Pac-Man</strong>. We shall simulate the game here using the OpenAI Gym. Let’s first import the <code class="language-plaintext highlighter-rouge">gym</code> library.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">gym</span>
</code></pre></div></div> <p>To create an instance of an environment we use the command <code class="language-plaintext highlighter-rouge">gym.make("env_name")</code></p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">env</span> <span class="o">=</span> <span class="n">gym</span><span class="p">.</span><span class="n">make</span><span class="p">(</span><span class="s">"MsPacman-v0"</span><span class="p">)</span>
<span class="n">env</span><span class="p">.</span><span class="n">reset</span><span class="p">()</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">env.reset()</code> initializes the environment to a default value.</p> <p>To see the agent play the game, we render the environment.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">env</span><span class="p">.</span><span class="n">render</span><span class="p">()</span>
<span class="o">&gt;&gt;&gt;</span><span class="bp">True</span>
</code></pre></div></div> <p><img style="float: right; margin-left: 25px; margin-right: auto; margin-top: auto; margin-bottom: auto;" src="/images/rl/pacman.png" desc="Ms. Pac-Man simulated"/></p> <p>As you can see, Ms. Pac-Man starts with 3 lives and has an initial score of 0.</p> <p>We then make our agent Pac-Man perform a random move.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">action</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">action_space</span><span class="p">.</span><span class="n">sample</span><span class="p">()</span>
<span class="n">new_state</span><span class="p">,</span> <span class="n">reward</span><span class="p">,</span> <span class="n">is_done</span><span class="p">,</span> <span class="n">info</span><span class="o">=</span><span class="n">env</span><span class="p">.</span><span class="n">step</span><span class="p">(</span><span class="n">action</span><span class="p">)</span>
</code></pre></div></div> <p>Let’s break this down. The <code class="language-plaintext highlighter-rouge">action_space</code> consists of the possible actions that can be done in an environment. <code class="language-plaintext highlighter-rouge">sample()</code> randomly chooses an action. Here we can see this:</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">env</span><span class="p">.</span><span class="n">action_space</span><span class="p">.</span><span class="n">n</span>         <span class="c1"># Number of possible actions the agent can take 
</span><span class="o">&gt;&gt;&gt;</span> <span class="mi">9</span> 
<span class="n">env</span><span class="p">.</span><span class="n">get_action_meanings</span><span class="p">()</span>  <span class="c1"># List of actions, NOOP means no actions. 
# All actions correspond to their own numbers eg. 0 -&gt; NOOP, 1-&gt; UP etc.
</span><span class="o">&gt;&gt;&gt;</span> <span class="p">[</span><span class="s">'NOOP'</span><span class="p">,</span> <span class="s">'UP'</span><span class="p">,</span> <span class="s">'RIGHT'</span><span class="p">,</span> <span class="s">'LEFT'</span><span class="p">,</span> <span class="s">'DOWN'</span><span class="p">,</span> <span class="s">'UPRIGHT'</span><span class="p">,</span> 
<span class="o">&gt;&gt;&gt;</span>  <span class="s">'UPLEFT'</span><span class="p">,</span> <span class="s">'DOWNRIGHT'</span><span class="p">,</span> <span class="s">'DOWNLEFT'</span><span class="p">]</span>
<span class="n">env</span><span class="p">.</span><span class="n">action_space</span><span class="p">.</span><span class="n">sample</span><span class="p">()</span>  <span class="c1"># Randomly selects a number. 2 -&gt; RIGHT
</span><span class="o">&gt;&gt;&gt;</span> <span class="mi">2</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">env.step(action)</code> is our agent taking the specified action. <code class="language-plaintext highlighter-rouge">new_state</code> refers to the new position of the environment after our agent has taken a step. <code class="language-plaintext highlighter-rouge">reward</code> is the immediate reward from the action; <code class="language-plaintext highlighter-rouge">is_done</code> is a boolean variable which signals the termination of the game; and <code class="language-plaintext highlighter-rouge">info</code> sometimes contains extra information. In this case:</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">print</span><span class="p">(</span><span class="n">info</span><span class="p">)</span>               <span class="c1"># Number of lives left 
</span><span class="o">&gt;&gt;&gt;</span> <span class="p">{</span><span class="s">'ale.lives'</span><span class="p">:</span> <span class="mi">3</span><span class="p">}</span>
</code></pre></div></div> <p>If we put all this in a while loop, our agent performs decently.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">is_done</span> <span class="o">=</span> <span class="bp">False</span>
<span class="k">while</span> <span class="ow">not</span> <span class="n">is_done</span><span class="p">:</span>                        <span class="c1"># Loop until game ends
</span>    <span class="n">action</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">action_space</span><span class="p">.</span><span class="n">sample</span><span class="p">()</span>
    <span class="n">new_space</span><span class="p">,</span> <span class="n">reward</span><span class="p">,</span> <span class="n">is_done</span><span class="p">,</span> <span class="n">info</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">step</span><span class="p">(</span><span class="n">action</span><span class="p">)</span>
    <span class="n">env</span><span class="p">.</span><span class="n">render</span><span class="p">()</span>
<span class="n">env</span><span class="p">.</span><span class="n">close</span><span class="p">()</span>                               <span class="c1"># Close the window
</span></code></pre></div></div> <p>Here is my simulation of running the game with random actions(Notice how the agent moves erratically).</p> <video width="500" height="375" controls=""> <source src="/images/rl/pacman.mp4" type="video/mp4"/> Random Action Gameplay </video> <h2 id="random-search-policy">Random Search Policy</h2> <p>As we can see above, randomly letting an agent do whatever isn’t really learning. Every now and again, however, the agent might find a good action. Given sufficient time (and luck), we might find the best set of actions; i.e., the optimal policy. This is done by repeating the process multiple times and selecting the set of actions with the highest reward. This happy-go-lucky approach is called the <strong>random search policy</strong>.</p> <p>To demonstrate this policy, we will look at a classic reinforcement learning task: CartPole. The job is to balance a pole on a moving cart for as long as possible. The pole falls if its angle increases by more than 12 degrees from the vertical, or if the cart moves 2.4 units from the origin. We are rewarded by 1 each timestep the pole doesn’t fall. The environment terminates after 200 timesteps.</p> <figure> <img src="/images/rl/cartpole.gif" desc="The CartPole"/> <figcaption>Look at the pole go!</figcaption> </figure> <p>Let’s first initialize the environment.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">env</span> <span class="o">=</span> <span class="n">gym</span><span class="p">.</span><span class="n">make</span><span class="p">(</span><span class="s">"CartPole-v0"</span><span class="p">)</span>
<span class="n">state</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">reset</span><span class="p">()</span>  <span class="c1"># Initialized to a random state
</span><span class="k">print</span><span class="p">(</span><span class="n">state</span><span class="p">)</span>      
<span class="o">&gt;&gt;&gt;</span><span class="p">[</span><span class="o">-</span><span class="mf">0.01926058</span><span class="p">,</span>  <span class="mf">0.03751507</span><span class="p">,</span> <span class="o">-</span><span class="mf">0.03824496</span><span class="p">,</span>  <span class="mf">0.00780216</span><span class="p">]</span>
</code></pre></div></div> <p>The 4 state values represent the cart position (Range: -2.4 to 2.4), cart velocity, pole angle in radians(Range: -0.209, 0.209) and pole velocity at the tip respectively. Also there are 2 actions, move left or move right.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">num_states</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">observation_space</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span>  <span class="c1"># Number of states: 4
</span><span class="n">num_actions</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">action_space</span><span class="p">.</span><span class="n">n</span>             <span class="c1"># Number of actions: 2
</span><span class="n">num_episodes</span> <span class="o">=</span> <span class="mi">1000</span>                          <span class="c1"># Number of trials
</span></code></pre></div></div> <p>Let’s first run a trial:</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Initializations
</span><span class="n">state</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">reset</span><span class="p">()</span>
<span class="n">total_reward</span> <span class="o">=</span> <span class="mi">0</span>
<span class="n">is_done</span> <span class="o">=</span> <span class="bp">False</span>
<span class="n">values</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">rand</span><span class="p">(</span><span class="n">num_states</span><span class="p">,</span> <span class="n">num_actions</span><span class="p">)</span>
<span class="k">print</span><span class="p">(</span><span class="n">values</span><span class="p">)</span>
<span class="o">&gt;&gt;&gt;</span><span class="n">tensor</span><span class="p">([[</span><span class="mf">0.4365</span><span class="p">,</span> <span class="mf">0.6748</span><span class="p">],</span>
        <span class="p">[</span><span class="mf">0.6149</span><span class="p">,</span> <span class="mf">0.6175</span><span class="p">],</span>
        <span class="p">[</span><span class="mf">0.9683</span><span class="p">,</span> <span class="mf">0.0224</span><span class="p">],</span>
        <span class="p">[</span><span class="mf">0.6787</span><span class="p">,</span> <span class="mf">0.0233</span><span class="p">]])</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">is_done</code> is a variable that is used to check whether the episode has terminated. We don’t know how much emphasis we should put on one measurement in the state, so we randomly assign importance. The PyTorch word for matrix is <code class="language-plaintext highlighter-rouge">tensor</code>.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">while</span> <span class="ow">not</span> <span class="n">is_done</span><span class="p">:</span>
    <span class="n">state</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">from_numpy</span><span class="p">(</span><span class="n">state</span><span class="p">).</span><span class="nb">float</span><span class="p">()</span>           <span class="c1"># For matrix multiplication
</span>    <span class="c1"># Index with highest value from 1x2 matrix is selected
</span>    <span class="n">action</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">argmax</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="n">matmul</span><span class="p">(</span><span class="n">state</span><span class="p">,</span> <span class="n">values</span><span class="p">)).</span><span class="n">item</span><span class="p">()</span>           
    <span class="n">state</span><span class="p">,</span> <span class="n">reward</span><span class="p">,</span> <span class="n">is_done</span><span class="p">,</span> <span class="n">info</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">step</span><span class="p">(</span><span class="n">action</span><span class="p">)</span>
    <span class="n">total_reward</span> <span class="o">+=</span> <span class="n">reward</span>
<span class="k">print</span><span class="p">(</span><span class="n">reward</span><span class="p">)</span>
<span class="o">&gt;&gt;&gt;</span> <span class="mi">24</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">from_numpy()</code> converts the state from a NumPy array to a Torch Tensor and <code class="language-plaintext highlighter-rouge">float()</code> ensures no precision is lost.This is done to prevent incompatibility errors. The next line is the most confusing here. For this let’s print everything.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">print</span><span class="p">(</span><span class="n">state</span><span class="p">)</span>     <span class="c1"># a simple 1x4 matrix
</span><span class="o">&gt;&gt;&gt;</span> <span class="n">tensor</span><span class="p">([</span> <span class="mf">0.02783453</span>  <span class="mf">0.04837403</span>  <span class="mf">0.02878761</span> <span class="o">-</span><span class="mf">0.00395011</span><span class="p">])</span>
<span class="k">print</span><span class="p">(</span><span class="n">values</span><span class="p">)</span>    <span class="c1"># a 4x2 matrix
</span><span class="o">&gt;&gt;&gt;</span><span class="n">tensor</span><span class="p">([[</span><span class="mf">0.4365</span><span class="p">,</span> <span class="mf">0.6748</span><span class="p">],</span>
        <span class="p">[</span><span class="mf">0.6149</span><span class="p">,</span> <span class="mf">0.6175</span><span class="p">],</span>
        <span class="p">[</span><span class="mf">0.9683</span><span class="p">,</span> <span class="mf">0.0224</span><span class="p">],</span>
        <span class="p">[</span><span class="mf">0.6787</span><span class="p">,</span> <span class="mf">0.0233</span><span class="p">]])</span>
<span class="c1"># The resulting multiplication product will be 1x4 * 4x2 = 1x2
</span><span class="n">weights</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">matmul</span><span class="p">(</span><span class="n">state</span><span class="p">,</span> <span class="n">values</span><span class="p">)</span>
<span class="k">print</span><span class="p">(</span><span class="n">weights</span><span class="p">)</span>
<span class="o">&gt;&gt;&gt;</span><span class="n">tensor</span><span class="p">([</span><span class="mf">0.0671</span><span class="p">,</span> <span class="mf">0.0492</span><span class="p">])</span>
</code></pre></div></div> <p>After multiplication we get the values 0.0671 and 0.0492. These are the weights of the actions. The next step is simply choosing the action with the higher weight.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">action</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">argmax</span><span class="p">(</span><span class="n">weights</span><span class="p">).</span><span class="n">item</span><span class="p">()</span>
<span class="k">print</span><span class="p">(</span><span class="n">action</span><span class="p">)</span>
<span class="o">&gt;&gt;&gt;</span> <span class="mi">0</span>
</code></pre></div></div> <p>As the 0th index had the higher value it was selected by <code class="language-plaintext highlighter-rouge">torch.argmax</code>. <code class="language-plaintext highlighter-rouge">.item()</code> is a PyTorch method that converts <code class="language-plaintext highlighter-rouge">tensor(0)</code> to <code class="language-plaintext highlighter-rouge">0</code>, i.e. converts a 1x1 matrix into a value.</p> <p>We convert this into a function for reusability.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">run_episode</span><span class="p">(</span><span class="n">env</span><span class="p">,</span> <span class="n">values</span><span class="p">):</span>
    <span class="n">state</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">reset</span><span class="p">()</span>
    <span class="n">total_reward</span> <span class="o">=</span> <span class="mi">0</span>
    <span class="n">is_done</span> <span class="o">=</span> <span class="bp">False</span>
    <span class="c1"># _ = True
</span>    <span class="k">while</span> <span class="ow">not</span> <span class="n">is_done</span><span class="p">:</span>
        <span class="n">state</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">from_numpy</span><span class="p">(</span><span class="n">state</span><span class="p">).</span><span class="nb">float</span><span class="p">()</span>           
        <span class="n">action</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">argmax</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="n">matmul</span><span class="p">(</span><span class="n">state</span><span class="p">,</span> <span class="n">values</span><span class="p">)).</span><span class="n">item</span><span class="p">()</span>          
        <span class="n">state</span><span class="p">,</span> <span class="n">reward</span><span class="p">,</span> <span class="n">is_done</span><span class="p">,</span> <span class="n">info</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="n">step</span><span class="p">(</span><span class="n">action</span><span class="p">)</span>
        <span class="n">total_reward</span> <span class="o">+=</span> <span class="n">reward</span>
    <span class="k">return</span> <span class="n">total_reward</span>
</code></pre></div></div> <p>We now run the random search process. Since we don’t know what the best set of values are, we just keep trying with random values until we hit the jackpot.</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">best_reward</span> <span class="o">=</span> <span class="mi">0</span>
<span class="n">best_values</span> <span class="o">=</span> <span class="bp">None</span>
<span class="n">total_rewards</span> <span class="o">=</span> <span class="p">[]</span>

<span class="k">for</span> <span class="n">episode</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="n">num_episodes</span><span class="p">):</span>
    <span class="n">values</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">rand</span><span class="p">(</span><span class="n">num_states</span><span class="p">,</span> <span class="n">num_actions</span><span class="p">)</span>  <span class="c1"># Random values
</span>    <span class="n">reward</span> <span class="o">=</span> <span class="n">run_episode</span><span class="p">(</span><span class="n">env</span><span class="p">,</span> <span class="n">values</span><span class="p">)</span>
    <span class="c1"># Keep track of the best rewards and values
</span>    <span class="k">if</span> <span class="n">reward</span> <span class="o">&gt;</span> <span class="n">best_reward</span><span class="p">:</span>
        <span class="n">best_reward</span> <span class="o">=</span> <span class="n">reward</span>
        <span class="n">best_values</span> <span class="o">=</span> <span class="n">values</span>
    <span class="c1"># Keep track of all rewards for comparison
</span>    <span class="n">total_rewards</span><span class="p">.</span><span class="n">append</span><span class="p">(</span><span class="n">reward</span><span class="p">)</span>

<span class="k">print</span><span class="p">(</span><span class="sa">f</span><span class="s">"Average reward, </span><span class="si">{</span><span class="n">num_episodes</span><span class="si">}</span><span class="s"> iterations: </span><span class="si">{</span><span class="nb">sum</span><span class="p">(</span><span class="n">total_rewards</span><span class="p">)</span> <span class="o">/</span> <span class="n">num_episodes</span><span class="si">}</span><span class="s">"</span><span class="p">)</span>
<span class="o">&gt;&gt;&gt;</span> <span class="n">Average</span> <span class="n">reward</span><span class="p">,</span> <span class="mi">1000</span> <span class="n">iterations</span><span class="p">:</span> <span class="mf">48.42</span>
<span class="k">print</span><span class="p">(</span><span class="n">best_reward</span><span class="p">)</span>
<span class="o">&gt;&gt;&gt;</span> <span class="mi">200</span>
</code></pre></div></div> <p>The average reward over a thousand iterations is rather low, while the best reward is 200, the maximum. If we run the episode 200 times with the best values, we get the following:</p> <div class="language-py highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">eval_num_episodes</span> <span class="o">=</span> <span class="mi">100</span>
<span class="n">eval_total_rewards</span> <span class="o">=</span> <span class="p">[]</span>
<span class="k">for</span> <span class="n">episode</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="n">eval_num_episodes</span><span class="p">):</span>
    <span class="c1"># Doing the same thing here
</span>    <span class="n">reward</span> <span class="o">=</span> <span class="n">run_episode</span><span class="p">(</span><span class="n">env</span><span class="p">,</span> <span class="n">best_values</span><span class="p">)</span>
    <span class="n">eval_total_rewards</span><span class="p">.</span><span class="n">append</span><span class="p">(</span><span class="n">reward</span><span class="p">)</span>  
    
<span class="k">print</span><span class="p">(</span><span class="sa">f</span><span class="s">"Average reward, </span><span class="si">{</span><span class="n">eval_num_episodes</span><span class="si">}</span><span class="s"> evaluation iterations:"</span><span class="p">,</span> 
<span class="sa">f</span><span class="s">"</span><span class="si">{</span><span class="nb">sum</span><span class="p">(</span><span class="n">eval_total_rewards</span><span class="p">)</span> <span class="o">/</span> <span class="n">eval_num_episodes</span><span class="si">}</span><span class="s">"</span><span class="p">)</span>
<span class="o">&gt;&gt;&gt;</span> <span class="n">Average</span> <span class="n">reward</span><span class="p">,</span> <span class="mi">100</span> <span class="n">iterations</span><span class="p">:</span> <span class="mf">158.86</span>
</code></pre></div></div> <p>This average reward is a lot higher than randomly selecting values<sup><a href="#myfootnote2">2</a></sup>.</p> <p>There is a very easy improvement to this algorithm which I’ll leave as an exercise. Instead of randomly selecting values each time, we can start with 1 matrix of random values in the beginning and updating it over time. This update can be in the form of adding some scaled random noise (say <code class="language-plaintext highlighter-rouge">scaling factor * torch.rand(num_states, num_actions)</code>) and we only update when the score improves. This method is known as <em>gradient ascent</em>.</p> <h2 id="conclusion">Conclusion</h2> <p>The OpenAI Gym provides a quick and simple way for everyone to get into RL. Unlike other hot AI fields like computer vision or natural language processing, a good portion of foundational reinforcement learning does not require any form of GPU support. In all subsequent articles I will stick with PyTorch, but you can feel free to do the same thing with NumPy. It is supposed to work, albeit more slowly.</p> <p>There are a large number of Atari games that can be simulated in the Gym. A full description and simulations can be found <a href="https://gym.openai.com/envs/#atari">here</a>. The Ms.Pac-Man environment is simulated above. Sadly, the game is too complex for something like Random Search (or even Gradient Ascent for that matter) to get correct.</p> <p>Random Search is a very basic way to solve a reinforcement learning problem. One could argue this isn’t even really learning, and to that I’d have to agree. Random search and gradient ascent work on simple environments like CartPole, but would absolutely tank in an Atari game. My next post will be on Dynamic Programming RL, a very powerful method that <strong>always converges</strong> to an optimal policy.</p> <h4 id="footnotes">Footnotes</h4> <p><a name="myfootnote1">[1]</a> While I won’t be explicitly going through PyTorch for brevity, I will explain the functions I use as they come up. Some level of experience with matrices and NumPy is inevitable.</p> <p><a name="myfootnote2">[2]</a> The value may vary from anywhere between 140 and 190 as the environment is always initialized randomly.</p>]]></content><author><name></name></author><category term="Reinforcement Learning"/><category term="OpenAI Gym"/><category term="Games and AI"/><category term="RL Basics"/><summary type="html"><![CDATA[Is this working out?]]></summary></entry><entry><title type="html">Reinforcement Learning - An Introduction</title><link href="https://alshedivat.github.io/al-folio/blog/rl_introduction/" rel="alternate" type="text/html" title="Reinforcement Learning - An Introduction"/><published>2021-05-31T00:00:00+00:00</published><updated>2021-05-31T00:00:00+00:00</updated><id>https://alshedivat.github.io/al-folio/blog/rl_intro_1</id><content type="html" xml:base="https://alshedivat.github.io/al-folio/blog/rl_introduction/"><![CDATA[<hr/> <meta property="og:image" content="https://raw.githubusercontent.com/shirsho-12/shirsho-12.github.io/master/images/rl/rl_theme.png"/> <meta property="og:url" content="https://shirsho-12.github.io"/> <meta property="og:description" content="An Introduction to Reinforcement Learning"/> <figure> <img src="/images/rl/xkcd_robot.png" desc="https://xkcd.com/1955/"/> <figcaption>XKCD #1955</figcaption> </figure> <p>According to Wikipedia, <em>Reinforcement learning (RL) is an area of machine learning concerned with how intelligent agents ought to take actions in an environment in order to maximize the notion of cumulative reward.</em><sup><a href="#myfootnote1">1</a></sup> . This sentence both says a lot, and at the same time tells very little. In layman’s terms, reinforcement learning is teaching a worker (the agent) to learn how to perform a task on its own. But how does an agent learn on its own? We leave the agent to its own devices. The agent is free to take whatever allowed action it wants and over time it may stumble onto a solution. We incentivize good solutions by giving the agent a task: maximize reward. This reward is specified by the programmer (a.k.a. us). Oftentimes we reward the agent for coming to a solution and apply penalties to longer solutions to encourage good solutions.</p> <p>Let’s first set out a few definitions:</p> <ul> <li><strong>Agent</strong>: The entity that is taking actions. In this case the agent is our algorithm that is trying to learn the optimal solution. A real world example would be a chess player.</li> <li><strong>Environment</strong>: The world the agent interacts with and is not in complete control over. The agent’s environment may not only be its outside world. For example, a robot dog’s environment not only consists of its surroundings, but also of its battery. After all, if our robot dog went outside in the yard and its battery died right before a snowstorm, it would be rather inconvenient.</li> <li><strong>State</strong>: The current condition of the environment.</li> <li><strong>Policy</strong>: The way an agent behaves in a certain time or position. In a more mathematical sense this is the mapping from the perceived state(what the agent sees) to an appropriate action.</li> <li><strong>Reward</strong>: This is the value we aim to maximize in the reinforcement learning problem. It is a measure of the <em>immediate</em> desirability of a state.</li> </ul> <figure> <img src="/images/rl/Spot_the_dog.jpg" desc="Spot the Dog"/> <figcaption padding-bottom="25px">Figure: Imagine your brand-new $74,500 Boston Dynamics robot dog Spot failed to keep its battery level in mind and got wasted in a blizzard. Tragic! </figcaption> </figure> <p>A simple example would be that of a chess position. The players are the agents, the chess board the environment and the reward victory. A move is an action, and the resulting chess position the state after move <em>t</em>.</p> <p>It should be noted that we are only rewarding final outcomes. We rarely, if ever, evaluate intermediate states or positions on our own, this is for the agent to figure out. This is primarily because human evaluations are often inaccurate. Give 10 people a difficult chess position and they may have wildly varying opinions on what’s best to do. We let the agent determine the desirability of a state in terms of its long term prospects, i.e. its <strong>value</strong>.</p> <p>Strictly speaking, values are predictions of future rewards. It is these values the agent continuously estimates, evaluates and re-evaluates. For the best course of action, the agent picks the actions with the best values in what we call a <em>greedy</em> manner.</p> <p>To summarize, the key feature of a reinforcement learning algorithm is its ability to interact with the environment, take feedback from it, and learn. Like a toddler learning how to walk, the algorithm(agent) continuously tries different actions until it finds the best(most rewarding) one.</p> <h2 id="supervised-unsupervised-and-reinforcement-learning">Supervised, Unsupervised and Reinforcement Learning</h2> <h3 id="supervised-vs-reinforcement-learning">Supervised vs Reinforcement Learning</h3> <p>Supervised learning is the process of learning from a set of labeled data. The algorithm’s job is to extract patterns and information from this labeled training data and generalize it. This simply does not work in complex, interactive situations. Labeling is also impractical in such problems as rewards are often delayed and the number of possible states numerous. Here, reinforcement learning algorithms can learn from experience.</p> <h3 id="unsupervised-vs-reinforcement-learning">Unsupervised vs Reinforcement Learning</h3> <p>Unsupervised learning attempts to cluster unlabeled data into groups. While exploring unseen structures is also relevant to reinforcement learning, the overarching goal is different. Reinforcement learning is goal-driven and reward-oriented.</p> <h2 id="connect-4">Connect-4</h2> <p>Let’s play a game of of Connect-4. Two players take turns to drop checkers on an <img src="https://render.githubusercontent.com/render/math?math=6\times 7"/> grid, with the goal of connecting four checkers in a row. The first player to do so wins. Each player only has 7 possible actions each turn, i.e. the column to drop a checker on. Such a simple game can lead to <strong>4,531,985,219,092 positions</strong><sup><a href="#myfootnote2">2</a></sup>! This makes classical methods such a minimax game theory, numerical optimization and dynamic programming methods<sup><a href="#myfootnote3">3</a></sup> unfeasible (these methods remain unfeasible for simpler games like tic-tac-toe).</p> <figure> <img src="/images/rl/Connect_Four.gif" desc="Connect-4"/> <figcaption padding-bottom="25px">Figure: The Connect-4 game. (Source: Wikipedia) </figcaption> </figure> <p>We would teach our agent to learn this game in the following way. First we set a table of numbers corresponding to the possible moves from each position (we only hold a small subset of these positions in memory at any given time for practical reasons). Each of these numbers correspond probability of us winning from that state, i.e. its value. We play thousands of games against the opponent. To select our moves(actions), we look at the resulting state from each move and look up their values from the table. Most of the time our agent plays greedily, only selecting the best values in the table. From time to time our agent will select random, sub-optimal moves. These <em>exploratory</em> moves are played so that we can learn from states that we wouldn’t have experienced otherwise. This is important because if our agent takes the same route over and over again, it may miss out on strategies that might have looked bad at first, but were much better down the line (insert deep philosophical message).</p> <p>During each game, we update the values associated with each state in an attempt to make them more accurate. This is done by adjusting the value of the last state we’d visited with respect to the resulting state. The following equation makes this clear.</p> <p>For an action <em>a</em> at state <em>s</em> resulting in state <em>s’</em>, the value <em>V(s,a)</em> is updated by</p> <p><img src="https://render.githubusercontent.com/render/math?math=V(s,a) \leftarrow V(s, a) %2B \alpha [V_{best}(s') - V(s,a)]"/></p> <p>where <img src="https://render.githubusercontent.com/render/math?math=\alpha"/> is a hyperparameter<sup><a href="#myfootnote4">4</a></sup> called the <em>step-size parameter</em>. The step-size parameter greatly affects the learning and convergence rate.</p> <p>This method (a form of <em>temporal difference learning</em>) converges over time if an appropriate <img src="https://render.githubusercontent.com/render/math?math=\alpha"/> value is selected. The resulting set of values is referred to as the <em>optimal values</em> <img src="https://render.githubusercontent.com/render/math?math=V^*"/> and the set of moves the optimal values dictate the <em>optimal policy</em> <strong>Q*</strong>.</p> <figure> <img src="/images/rl/backup_diag_connect_4.png" desc="Backup Digram: Connect 4"/> <figcaption padding-bottom="25px">Figure: A sequence of moves from the starting position. The solid lines represent moves taken in game; and the dashed lines show candidate moves. The second move e was an exploratory move. Exploratory moves do not directly result in learning as the optimal choice isn't updated. (Source: Reinforcement Learning: An Introduction. Sutton &amp; Barlo) </figcaption> </figure> <p>As you can see, this reinforcement learning approach to the game resulted in an agent which is able to learn the game pretty well. The agent can not only find the best action in a given position, but can also apply foresight. This ability to apply deep foresight is what makes reinforcement learning not only useful in games, but also in real world applications such as supply chain logistics and process control.</p> <h2 id="conclusion">Conclusion</h2> <p>Reinforcement learning is a computational approach to solving problems with clear goals by interacting with its external environment without the need for any additional supervision. The interaction between a learning agent and its environment in the form of states, actions, and rewards leads to a clean, powerful goal-driven framework.</p> <p>There exists another method of solving reinforcement learning problems called genetic algorithms. These algorithms apply principles such as evolution and natural selection to solve problems. Genetic algorithms deviate from the methods which will be discussed in this series in that genetic algorithms do not use value functions or learn in a single generation.</p> <p>Value functions are at the heart of reinforcement learning, and it is what my future articles will be about. I’ll be going through Dynamic Programming, Monte Carlo and Temporal Difference Learning methods soon. But before that, my next post will be about setting up the OpenAI Gym and playing with the CartPole and Atari Space Invaders environments.</p> <h4 id="footnotes">Footnotes</h4> <p><a name="myfootnote1">[1]</a> Hu, J.; Niu, H.; Carrasco, J.; Lennox, B.; Arvin, F. (2020). <a href="https://ieeexplore.ieee.org/abstract/document/9244647">“Voronoi-Based Multi-Robot Autonomous Exploration in Unknown Environments via Deep Reinforcement Learning”</a>. IEEE Transactions on Vehicular Technology. 69 (12): 14413-14423.</p> <p><a name="myfootnote2">[2]</a> <a href="https://oeis.org/A212693">Number of legal 7 X 6 Connect-Four positions after n plies</a>, Online Encyclopedia of Integer Sequences.</p> <p><a name="myfootnote3">[3]</a> Dynamic programming can be used to solve reinforcement learning problems. However, they are only useful in cases where the state size is small and complete environment knowledge is available.</p> <p><a name="myfootnote4">[4]</a> A hyperparameter is a value which the programmer has to set on his or her own and these may oftentimes greatly influence a particular aspect of the algorithm.</p>]]></content><author><name></name></author><category term="Reinforcement Learning"/><category term="RL Basics"/><category term="Artificial Intelligence"/><category term="Games and AI"/><summary type="html"><![CDATA[]]></summary></entry><entry><title type="html">Reinforcement Learning - A Preface</title><link href="https://alshedivat.github.io/al-folio/blog/rl_p/" rel="alternate" type="text/html" title="Reinforcement Learning - A Preface"/><published>2021-05-30T00:00:00+00:00</published><updated>2021-05-30T00:00:00+00:00</updated><id>https://alshedivat.github.io/al-folio/blog/rl_preface</id><content type="html" xml:base="https://alshedivat.github.io/al-folio/blog/rl_p/"><![CDATA[<hr/> <p>Over the last month (May 2021), I have been delving into the world of reinforcement learning (RL). The following series of posts will be about my ongoing journey in this field highlighting the things I am learning. My goal here is to help you see this magical world of RL and how it is rapidly shaping the world around us.</p> <p>So what is <strong>reinforcement learning</strong>?</p>]]></content><author><name></name></author><category term="Reinforcement Learning"/><category term="RL Basics"/><summary type="html"><![CDATA[]]></summary></entry><entry><title type="html">a post with github metadata</title><link href="https://alshedivat.github.io/al-folio/blog/2020/github-metadata/" rel="alternate" type="text/html" title="a post with github metadata"/><published>2020-09-28T21:01:00+00:00</published><updated>2020-09-28T21:01:00+00:00</updated><id>https://alshedivat.github.io/al-folio/blog/2020/github-metadata</id><content type="html" xml:base="https://alshedivat.github.io/al-folio/blog/2020/github-metadata/"><![CDATA[<p>A sample blog page that demonstrates the accessing of github meta data.</p> <h2 id="what-does-github-metadata-do">What does Github-MetaData do?</h2> <ul> <li>Propagates the site.github namespace with repository metadata</li> <li>Setting site variables : <ul> <li>site.title</li> <li>site.description</li> <li>site.url</li> <li>site.baseurl</li> </ul> </li> <li>Accessing the metadata - duh.</li> <li>Generating edittable links.</li> </ul> <h2 id="additional-reading">Additional Reading</h2> <ul> <li>If you’re recieving incorrect/missing data, you may need to perform a Github API<a href="https://github.com/jekyll/github-metadata/blob/master/docs/authentication.md"> authentication</a>.</li> <li>Go through this <a href="https://jekyll.github.io/github-metadata/">README</a> for more details on the topic.</li> <li><a href="https://github.com/jekyll/github-metadata/blob/master/docs/site.github.md">This page</a> highlights all the feilds you can access with github-metadata. <br/></li> </ul> <h2 id="example-metadata">Example MetaData</h2> <ul> <li>Host Name :</li> <li>URL :</li> <li>BaseURL :</li> <li>Archived :</li> <li>Contributors :</li> </ul>]]></content><author><name></name></author><category term="sample-posts"/><category term="external-services"/><summary type="html"><![CDATA[a quick run down on accessing github metadata.]]></summary></entry><entry><title type="html">LeNet5 - The First ConvNet</title><link href="https://alshedivat.github.io/al-folio/blog/lenet/" rel="alternate" type="text/html" title="LeNet5 - The First ConvNet"/><published>2020-08-03T00:00:00+00:00</published><updated>2020-08-03T00:00:00+00:00</updated><id>https://alshedivat.github.io/al-folio/blog/lenet5</id><content type="html" xml:base="https://alshedivat.github.io/al-folio/blog/lenet/"><![CDATA[<hr/> <p>Welcome to my very first blog post. This will be the beginning of a series of posts on some of the fundamental architectures in all of Deep Learning. This post will disseminate the now <strong>22-year old</strong> paper by Yann LeCun <em>et al.</em> titled <a href="http://yann.lecun.com/exdb/publis/pdf/lecun-01a.pdf">Gradient-Based Learning Applied to Document Recognition</a>.</p> <p>I’ll be focusing on the LeNet-5 architecture over Graph Transformer Networks(GTN), Space Displacement Neural Networks(SDNN) and some other things brought up in the 46-page paper because of their complexity and that LeNet-5 outperformed these approaches. (SDNNs are now a relic of the past).</p> <p>LeNet-5 laid down the framework of today’s AI landscape. It is the first paper that applied the modern Convolutional Neural Network with gradient-based learning to a real-world application. Prior to LeNet-5, convolutional neural networks rarely had any applications. Instead, static hand-crafted features and Support Vector Machines(SVMs) were the norm.</p> <p>Convolutional Neural Networks (referred to as CNNs or ConvNets) are a series of giant, mysterious blocks of matrix multiplications, non-linearity functions, and differential calculus and statistics. I will cover the individual sections of a neural network in future blog posts. I will, on the other hand, lay down a list of the essential blocks below.</p> <ul> <li>Convolutions</li> <li>Non-Linearities</li> <li>Backpropagation</li> <li>Objective/Loss Functions</li> <li>Optimizer Functions</li> <li>Layer Types (Pooling, Fully-Connected, Convolutional etc.)</li> </ul> <p>While there are many other concepts that merit being in the list (like Batch Normalization for example), the 6 above are essential to all neural networks.</p> <p>##LeNet-5## LeNet-5, as the name suggests, is a 5 layer convolutional neural network created by Yann LeCun, Léon Bottou, Yoshua Bengio and Patric Haffner. The model was first used to classify handwritten digits in the MNIST dataset. I will be recreating this model using PyTorch. All relevant scripts will be available on my <a href="www.google.com">GitHub repository</a>.</p> <p>####Libraries#### Before we get to the core implementation details, we need to import some libraries. The libraries are completely open-source. I will be using PyTorch in all of my work.</p> <p>import torch import torch.nn as nn import numpy as np import matplotlib.pyplot as plt import torchvision</p> <p><em>These are standard library imports required for all models written in PyTorch.</em></p> <p><img src="/images/lenet/lenet_architecture.PNG" alt="LeNet Architecture"/> LeNet-5 in all its glory being used in digit recognition. Image taken from the original paper.</p>]]></content><author><name></name></author><category term="Deep Learning"/><category term="Architectures"/><summary type="html"><![CDATA[]]></summary></entry><entry><title type="html">a post with images</title><link href="https://alshedivat.github.io/al-folio/blog/2015/images/" rel="alternate" type="text/html" title="a post with images"/><published>2015-05-15T21:01:00+00:00</published><updated>2015-05-15T21:01:00+00:00</updated><id>https://alshedivat.github.io/al-folio/blog/2015/images</id><content type="html" xml:base="https://alshedivat.github.io/al-folio/blog/2015/images/"><![CDATA[<p>This is an example post with image galleries.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/al-folio/assets/img/9-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/al-folio/assets/img/9-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/al-folio/assets/img/9-1400.webp"/> <img src="/al-folio/assets/img/9.jpg" class="img-fluid rounded z-depth-1" width="auto" height="auto" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/al-folio/assets/img/7-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/al-folio/assets/img/7-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/al-folio/assets/img/7-1400.webp"/> <img src="/al-folio/assets/img/7.jpg" class="img-fluid rounded z-depth-1" width="auto" height="auto" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> A simple, elegant caption looks good between image rows, after each row, or doesn't have to be there at all. </div> <p>Images can be made zoomable. Simply add <code class="language-plaintext highlighter-rouge">data-zoomable</code> to <code class="language-plaintext highlighter-rouge">&lt;img&gt;</code> tags that you want to make zoomable.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/al-folio/assets/img/8-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/al-folio/assets/img/8-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/al-folio/assets/img/8-1400.webp"/> <img src="/al-folio/assets/img/8.jpg" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/al-folio/assets/img/10-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/al-folio/assets/img/10-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/al-folio/assets/img/10-1400.webp"/> <img src="/al-folio/assets/img/10.jpg" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>The rest of the images in this post are all zoomable, arranged into different mini-galleries.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/al-folio/assets/img/11-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/al-folio/assets/img/11-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/al-folio/assets/img/11-1400.webp"/> <img src="/al-folio/assets/img/11.jpg" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/al-folio/assets/img/12-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/al-folio/assets/img/12-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/al-folio/assets/img/12-1400.webp"/> <img src="/al-folio/assets/img/12.jpg" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" media="(max-width: 480px)" srcset="/al-folio/assets/img/7-480.webp"/> <source class="responsive-img-srcset" media="(max-width: 800px)" srcset="/al-folio/assets/img/7-800.webp"/> <source class="responsive-img-srcset" media="(max-width: 1400px)" srcset="/al-folio/assets/img/7-1400.webp"/> <img src="/al-folio/assets/img/7.jpg" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div>]]></content><author><name></name></author><category term="sample-posts"/><category term="formatting"/><category term="images"/><summary type="html"><![CDATA[this is what included images could look like]]></summary></entry></feed>